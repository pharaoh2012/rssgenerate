
        <h2 class="post-title">
            <a id="cb_post_title_url" class="postTitle2 vertical-middle" href="https://www.cnblogs.com/owlman/p/19256216" title="发布于 2025-11-22 12:10">
    <span role="heading" aria-level="2">LaTeX学习笔记：文档排版基础</span>
    

</a>

        </h2>
        <div id="cnblogs_post_body" class="blogpost-body cnblogs-markdown">
<h1 id="latex-学习笔记文档排版基础">LaTeX 学习笔记：文档排版基础</h1>
<p>在撰写学术论文、技术报告或专业著作时，排版的质量往往决定了作品给人的第一印象。对于计算机科学、工程技术、数理化等领域而言，文档中不仅包含大量数学公式，还常常涉及图表、参考文献等复杂结构。正如我们在《[[LaTeX学习笔记：开场白与索引]]》一文中所提到的那样，计算机科学家们正是为了应对这些复杂的排版需求而开发了 <span class="math inline">\(\LaTeX\)</span>。</p>
<p>在本篇笔记中，我们将重点介绍 <span class="math inline">\(\LaTeX\)</span> 在专业排版中的基础技巧，帮助提升学术文档的可读性与专业气质。下面，就让我们先从文档的排版结构开始吧。</p>
<h2 id="定义排版结构">定义排版结构</h2>
<p>在 <span class="math inline">\(\LaTeX\)</span> 中，文档的整体排版结构是通过一系列<strong>结构性命令</strong>（structural commands）来定义的。这些命令用于给文档指定排版类型，加载功能扩展包，设置页面版式，设置标题、作者等元信息，并生成文档的开篇部分。最常用的命令包括：</p>
<ul>
<li><code>\documentclass</code>：用于指定文档所要采用的排版类型；</li>
<li><code>\usepackage</code>：用于通过加载宏包的方式来获得扩展功能；</li>
<li><code>\geometry</code>：用于设置文档的页面尺寸与页边距等页面版式信息</li>
<li><code>\title</code>、<code>\author</code>、<code>\date</code>：用于定义文档的标题、作者等元信息；</li>
</ul>
<p>接下来，我将会按照功能逐一介绍这些命令的使用方法。</p>
<h3 id="指定排版类型">指定排版类型</h3>
<p>在使用 <span class="math inline">\(\LaTeX\)</span> 语言定义一个文档的排版结构时，我们需要先在其源文件的第一行中使用<code>\documentclass</code>命令指定要采用的排版类型。换而言之，我们需要首先指定该文档的排版规格属于短篇文章、学术报告、信件、书籍，还是他类型的文献。在程序员的日常工作中，较为常用排版类型主要可分为以下几种：</p>
<ol>
<li>
<p><strong>标准文档类</strong>，由 <span class="math inline">\(\LaTeX\)</span> 核心包提供，主要包括以下四个类：</p>
<ul>
<li><code>article</code>：用于排版短篇文章，包括期刊投稿、技术文档等；</li>
<li><code>report</code>：用于排版学术报告，包括毕业论文、技术报告等；</li>
<li><code>book</code>：用于排版书籍类文档，例如专著、教材等；</li>
<li><code>letter</code>：用于排版信件类文档，包括个人书信、商务信函等；</li>
</ul>
</li>
<li>
<p><strong>扩展文档类</strong>：由<code>extsizes</code>宏包提供，提供有<code>extarticle</code>、<code>extreport</code>、<code>extbook</code>三个类，它们分别是上述前三个标准文档类的扩展版，主要区别是支持更大的字号选项（如 <code>14pt</code>、<code>17pt</code>、<code>20pt</code>）</p>
</li>
</ol>
<p>3.<strong>中文文档类</strong>：由<code>ctex</code>宏包提供，提供有<code>ctexart</code>、<code>ctexrep</code>、<code>ctexbook</code>、<code>ctexletter</code>四个类，它们分别是上述四个标准文档类的中文增强版本，可自动配置中文字体、字号、段落间距与标题格式，更适合中文排版。值得一提的是：在新版 CTeX 宏包中（自 2.0 以后），<code>ctexletter</code>类尽管仍然存在，但默认情况下不会随发行版安装，读者如有需要可通过 texdoc ctex 查阅详细信息。</p>
<p>下面，让我们来具体演示一下<code>\documentclass</code>命令的使用。假设，你现在想要让目标文档按照<code>ctexart</code>类型来排版，那么就需要在其 <span class="math inline">\(\LaTeX\)</span> 源文件的开头添加以下命令：</p>
<pre><code class="language-tex">\documentclass{ctexart}
</code></pre>
<p>当然，该命令除了指定文档的排版类型之外，还可以通过一些可选参数来指定文档的某些排版细节。例如像下面这样：</p>
<pre><code class="language-tex">\documentclass[12pt,oneside]{ctexart}
</code></pre>
<p>在上述命令的中括号里，参数<code>12pt</code>用于将文档的字体大小设置为 12 磅（point），而参数<code>oneside</code>则用于指定该文档将采用单面打印的方式输出。总体来说，<code>\documentclass</code>命令的可选参数主要可分为以下几大类：</p>
<ol>
<li>
<p><strong>字体与字号参数</strong>：用于设置文档的字体大小，除了之前提到的 <code>12pt</code> 外，还包括如下常见选项：</p>
<ul>
<li><code>10pt</code>（默认值）：适用于一般文档；</li>
<li><code>11pt</code>：适用于需要稍大字体的论文或报告；</li>
<li><code>14pt</code>, <code>17pt</code>, <code>20pt</code>：部分扩展类支持更大的字号；</li>
</ul>
<p>需要再次强调的是：<code>14pt</code>、<code>17pt</code>、<code>20pt</code>等较大字号仅在<code>extarticle</code>等扩展类中可用。另外，对于中文类文档（如 <code>ctexart</code>），也可通过 <code>zihao</code> 参数控制字号，例如：</p>
<pre><code class="language-tex">\documentclass[zihao=-4]{ctexart} % 设置中文字号
</code></pre>
<p>这会根据中文字号标准（初号、四号等）调整排版比例，更符合中文出版规范。</p>
</li>
<li>
<p><strong>页面尺寸参数</strong>：用于设置文档的页面尺寸，常见选项主要包括：</p>
<ul>
<li><code>a4paper</code>：适用于 A4 纸；</li>
<li><code>letterpaper</code>：适用于 US Letter 纸；</li>
<li><code>a5paper</code>：适用于 A5 纸；</li>
<li><code>b5paper</code>：适用于 B5 纸；</li>
<li><code>executivepaper</code>：适用于 Executive 纸；</li>
<li><code>legalpaper</code>：适用于 Legal 纸；</li>
</ul>
</li>
<li>
<p><strong>页面布局参数</strong>：用于设置文档的页面布局，常见选项主要包括：</p>
<ul>
<li><code>titlepage/notitlepage</code>：控制是否生成独立标题页（<code>report</code>和<code>book</code>这两个类默认采用<code>titlepage</code>，<code>article</code>类默认则不是）；</li>
<li><code>openright/openany</code>：控制章节起始页是否必须在奇数页（书籍排版常用）；</li>
<li><code>twocolumn/onecolumn</code>：双栏或单栏排版；</li>
<li><code>draft/final</code>：在草稿模式下，LaTeX 会标出溢出行、图片留空；</li>
<li><code>fleqn/leqno</code>：左对齐公式或在左侧编号；</li>
</ul>
</li>
<li>
<p><strong>打印与装订参数</strong>：用于设置文档的打印与装订方式，常见选项主要包括：</p>
<ul>
<li><code>oneside/twoside</code>：控制单双面 排版（<code>book</code>类默认采用<code>twoside</code>）；</li>
<li><code>landscape</code>：横向排版（与 PDF 输出相配合时尤其有用）。</li>
</ul>
</li>
</ol>
<p>当然，除了上面列出的排版类型及其相关参数之外，一些特定的学术期刊或机构可能会要求我们按照特排版类型来排版，例如<code>IEEEtran</code>、<code>acm_proc_article-sp</code>等。虽然在通常情况下，这些来自第三方的排版类型也只需使用<code>\documentclass</code>命令来指定即可，但在某些特定情况下，如果想让这些排版类型充分发挥作用，还需通过<code>\usepackage</code>命令来加载它们所通过的功能扩展包，以扩展 <span class="math inline">\(\LaTeX\)</span> 的排版功能，下面就让我们继续来看这个命令的使用方法吧。</p>
<h3 id="加载功能扩展包">加载功能扩展包</h3>
<p>从程序员的角度，我们可以将 <span class="math inline">\(\LaTeX\)</span> 排版系统中的<code>\usepackage</code>命令理解成 Python 中的<code>import</code>语句，C/C++ 语言中的<code>#include</code>语句，它们的作用是在当前源文件中引入 <span class="math inline">\(\LaTeX\)</span> 自带或者第三方提供的扩展包，以便获得特定的功能。在 <span class="math inline">\(\LaTeX\)</span> 语言中，这些扩展包都是以“宏包（package）”的形式提供的，例如<code>amsmath</code>、<code>graphicx</code>、<code>hyperref</code>等。具体来说，就是我们需要在使用<code>\documentclass</code>命令指定了当前文档所要采用的排版类型之后，就紧接着使用<code>\usepackage</code>命令来加载我们需要的宏包，以便获得这些宏包提供的扩展功能。</p>
<p>例如，如果我们基于某种灵活性方面的考虑，既想使用标准类<code>article</code>排版，又想在文档中使用中文，就可以通过<code>\usepackage</code>命令加载<code>ctex</code>这个宏包来实现，像下面这样：</p>
<pre><code class="language-tex">\documentclass{article}
\usepackage{ctex}
% 其他命令 ...
</code></pre>
<p>除此之外，为了达到专业排版效果，我们通常还会推荐加载以下宏包（具体请根据实际需求来进行选择）：</p>
<pre><code class="language-tex">% 基础
\usepackage{ctex}        % 中文支持
\usepackage{geometry}    % 页面设置

% 数学相关
\usepackage{amsmath,amssymb}

% 图形相关
\usepackage{graphicx,float,caption,subcaption}

% 表格相关
\usepackage{booktabs}

% 超链接与引用相关
\usepackage{hyperref,cleveref}

% 文献管理相关
\usepackage{biblatex}

% 微排版相关
\usepackage{microtype}
</code></pre>
<p>当然，在加载宏包时，需要注意遵循“少而精”原则：只加载必要宏包即可，以避免发生冲突或冗余。例如，在上述推荐中，如果我们事前已经加载了<code>ctex</code>宏包，那么<code>inputenc</code>、<code>fontenc</code>、<code>lmodern</code>这几个宏包就无需再加载了。因为使用<code>ctex</code>宏包时，我们采用的排版引擎大概率是 XeLaTeX 或 LuaLaTeX，它们已经内置了 UTF-8 编码、T1 字体编码以及 Latin Modern 字体，无需再额外加载。</p>
<p>另外，加载宏包也需要特别注意一下加载的顺序，因为某些宏包之间存在着特定的依赖关系，对加载的顺序是敏感的，例如，我们通常建议先加载数学宏包（<code>amsmath</code>、<code>amssymb</code>），再加载<code>hyperref</code>；而<code>cleveref</code>则必须在<code>hyperref</code>之后加载。总而言之，读者最好在具体使用之前查阅一下这些宏包的文档，以了解它们之间的依赖关系。</p>
<h3 id="设置页面版式">设置页面版式</h3>
<p>之前在介绍<code>\documentclass</code>命令时，我们曾提到过一些与页面版式相关的可选参数，例如<code>a4paper</code>、<code>twoside</code>、<code>oneside</code>等。这些参数属于文档类中的全局配置接口，只能对排版进行一些<strong>粗粒度的默认设置</strong>，例如采用的纸张类型、单/双面打印模式等，但并不能精确控制页面边距或版心尺寸。</p>
<p>如果希望文档在阅读体验和排版细节上达到专业水准，就需要对页面布局进行更精细的控制。通常的做法是先通过<code>\usepackage</code>命令加载名为<code>geometry</code>的宏包，然后再使用<code>\geometry</code>命令设置页面的具体版式。例如像下面这样</p>
<pre><code class="language-tex">\documentclass{article}
\usepackage{geometry} % 加载 geometry 宏包

\geometry{
    a4paper,       % 设置纸张尺寸
    left=3cm,      % 左页边距
    right=3cm,     % 右页边距
    top=3cm,       % 上页边距
    bottom=3cm     % 下页边距
}
</code></pre>
<p>与<code>\documentclass</code>命令中的可选参数相比，<code>\geometry</code>命令提供了更细粒度的页面布局控制，内容包括页边距、版心大小、装订线、页眉页脚高度等。因此，它广泛应用于技术文档、学术论文与书籍排版中。下面，让我们来看一个用于给专业书籍排版的示例：</p>
<pre><code class="language-tex">\documentclass[12pt,twoside]{book} % 双面打印常用于书籍排版
\usepackage{geometry}

\geometry{
    paper=a5paper,          % 书籍常用 A5 尺寸
    inner=2.8cm,            % 订口（内侧边距）
    outer=2.2cm,            % 外侧边距
    top=2.5cm,              % 上边距
    bottom=2.5cm,           % 下边距
    headheight=14pt,        % 页眉高度
    headsep=0.8cm,          % 页眉到正文距离
    footskip=1.2cm          % 页脚到正文距离
}
</code></pre>
<p>在上面这个示例中，我们通过<code>\geometry</code>命令对文档进行了精确的页面布局设置，其中：</p>
<ul>
<li>参数<code>twoside</code>用于将书籍设置为“奇偶页镜像布局”，即将内侧边距设置得更大一些，以便装订。</li>
<li>参数<code>inner/outer</code>是专为双页面打印的选项，用于取代参数<code>left/right</code>相对死板的页边距设置。</li>
<li><code>headsep</code>、<code>footskip</code>等参数的作用是用于细调页眉页脚与正文间的距离。</li>
</ul>
<p>通过这种方式，我们可以在维持文档类整体结构的基础上，对页面版式进行专业级的精细化定义。当然，<code>\geometry</code>命令可设置的参数远不止这些，如果读者想了解更多该命令的参数，可以查阅该<code>geometry</code>宏包的官方文档。</p>
<p>另外在 <span class="math inline">\(\LaTeX\)</span> 语言中，关于版式设置的命令也并不只有<code>\geometry</code>。还包括用于设置行距、页眉页脚、分栏等多方面的命令或宏包。下面让我们来做个简单的了解：</p>
<ol>
<li>
<p><strong><code>\linespread</code>命令</strong>：主要利用行间距调整正文可读性，常用于论文或技术报告。例如<code>\linespread{1.2}</code>。通常情况下，<code>1.0</code>属于正常行间距，大于<code>1.0</code>则是选择拉开行距，小于<code>1.0</code>则属于紧凑行距。当然，相同的功能也可以通过加载 <code>setspace</code> 宏包来实现，例如：</p>
<pre><code class="language-tex">\usepackage{setspace}
\singlespacing     % 单倍行距
\onehalfspacing    % 1.5 倍行距
\doublespacing     % 双倍行距
</code></pre>
</li>
<li>
<p><strong><code>\pagestyle</code>命令</strong>：用于设置页眉页脚的样式，它的参数可以是<code>plain</code>、<code>empty</code>、<code>headings</code>、<code>myheadings</code>等。其中，<code>plain</code>是默认的页眉页脚样式（只显示页码）；<code>empty</code>则不显示页眉页脚；<code>headings</code>则会在页眉中显示章节标题和页码；<code>myheadings</code>则允许用户自定义页眉内容。同样的，<code>\pagestyle</code>命令的功能也可以通过加载<code>fancyhdr</code>宏包来得到扩展，例如：</p>
<pre><code class="language-tex">\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % 清空页眉页脚
\fancyhead[L]{左页眉}
\fancyhead[C]{中页眉}
\fancyhead[R]{右页眉}
\fancyfoot[L]{左页脚}
\fancyfoot[C]{中页脚}
\fancyfoot[R]{右页脚}
</code></pre>
<p>在上述示例中：</p>
<ul>
<li><code>\fancyhf{}</code>命令用于清空页眉页脚；</li>
<li><code>\fancyhead</code>命令用于设置页眉内容；</li>
<li><code>\fancyfoot</code>命令用于设置页脚内容。</li>
<li><code>[L]</code>、<code>[C]</code>、<code>[R]</code>分别表示左、中、右三个位置。</li>
<li><code>fancy</code>参数表示采用<code>fancyhdr</code>宏包提供的页眉页脚样式。</li>
</ul>
</li>
<li>
<p><strong><code>\twocolumn</code>、<code>\onecolumn</code></strong>命令：用于控制正文是否双栏排版。例如<code>\twocolumn</code>是双栏排版，<code>\onecolumn</code>则是单栏排版。</p>
</li>
<li>
<p><strong><code>\topmargin</code>、<code>\oddsidemargin</code>等命令</strong>：它们是传统 LaTeX 提供的命令，可直接控制页边距，例如：</p>
<pre><code class="language-tex">\setlength{\topmargin}{-0.5in}
\setlength{\oddsidemargin}{0.2in}
\setlength{\textwidth}{6in}
\setlength{\textheight}{9in}
</code></pre>
<p><strong>请注意</strong>：上述命令使用起来不如<code>\geometry</code>命令直观，很容易出错，现代文档推荐使用<code>geometry</code>宏包提供的功能。</p>
</li>
</ol>
<p>除了上面列出的这些命令和相关宏包之外，与版式相关的宏包还包括以下这些，读者如有需要可以查阅相关文档：</p>
<ul>
<li><code>titlesec</code>：用于设置章节标题样式；</li>
<li><code>caption</code>：用于设置图表标题格式；</li>
<li><code>parskip</code>：用于设置段落间的垂直间距；</li>
<li><code>microtype</code>：用于字间距微调，提高排版质量；</li>
<li><code>layout</code>：用于设置可视化页面布局;</li>
<li><code>typearea</code>：用于设置页面版式，与<code>geometry</code>类似，但功能更强大。</li>
</ul>
<h3 id="设置文档元信息">设置文档元信息</h3>
<p>在 <span class="math inline">\(\LaTeX\)</span> 语言中，文档可设置的元信息取决于其采用的排版类型。例如，就之前提到的<code>article</code>、<code>report</code>、<code>book</code>等文档类而言，我们分别可通过<code>\title</code>、<code>\author</code>、<code>\date</code>这三个命令来设置文档的标题、作者与发表日期，这些信息通常会显示在文档的标题页中。例如，如果我们想对张三撰写的一篇题为“LaTeX 快速入门”的文档进行排版，就可以像这样设置它的元信息：</p>
<pre><code class="language-tex">\documentclass[oneside, 12pt, a4paper]{article}
\usepackage{ctex} % 支持中文

% 设置文档元信息
\title{\LaTeX 快速入门} % 设置文档标题
\author{张三}           % 设置文档作者，如果有多个作者，可使用“\and”分隔，
                        % 例如：“张三\and 李四”
\date{\today}           % 设置文档日期，如果不想设置，可以写 \date{}

% 开始组织文档内容
\begin{document}
    \maketitle          % 生成标题页
\end{document}
</code></pre>
<p>上述代码所设置的这些信息会被保存在  <span class="math inline">\(\LaTeX\)</span> 内部定义的结构中，并在执行<code>\maketitle</code>时用于排版标题页。如图1所示：</p>

<p><img alt="图1：标题页示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122115601226-764808201.png" class="lazyload"></p>
<p>除此之外，如果我们还想在文档的元信息中加入诸如摘要、关键字等学术论文中常见的内容，就必须借助第三方提供的文档类或宏包来进行扩展了，下面是一些常见实例：</p>
<ul>
<li><code>beamer</code>文档类提供了用于设置副标题（<code>\subtitle</code>命令）、机构（<code>\institute</code>命令）、图标（<code>\logo</code>命令）等元信息的扩展接口；</li>
<li><code>IEEEtran</code>文档类提供了用于设置关键字（<code>IEEEkeywords</code>环境）、摘要（<code>abstract</code>环境）等元信息的扩展接口；</li>
<li><code>memoir</code>文档类提供了<code>\publishers</code>、<code>\affiliation</code>、<code>\subject</code>等更丰富的标题页定制命令，分别用于设置出版商、机构、主题等元信息；</li>
</ul>
<p>换而言之，如果要设置相对复杂的元信息，需要根据对应文档所采用的排版类型及其相关的文档说明来设置，并不存在通用的解决方案。另外，需要额外说明的一点是：<strong>标题页上显示的元信息</strong>（例如 <code>\title</code>命令输出的内容）与<strong>PDF 文件本身的元信息</strong> 并不是同一套东西。后者常见于 PDF 阅读器的“文件属性”界面，需要使用<code>hyperref</code>宏包来设置，例如：</p>
<pre><code class="language-tex">\usepackage{hyperref}
\hypersetup{
    pdftitle={LaTeX 快速入门},
    pdfauthor={张三},
    pdfsubject={LaTeX 教程},
    pdfkeywords={LaTeX, 排版, 入门}
}
</code></pre>
<p>这些属性不会根据<code>\title</code>或<code>\author</code>命令来自动推断，因此通常需要手动指定。总体来说，<strong><span class="math inline">\(\LaTeX\)</span> 的元信息系统是“按需扩展”的</strong>：基础类只提供最小集合，而不同的文档类和宏包根据场景添加自己的元数据接口。这也是 <span class="math inline">\(\LaTeX\)</span> 在学术论文、演示文稿、技术报告等场景中具有高度可扩展性的原因。</p>
<h2 id="组织文档内容">组织文档内容</h2>
<p>一般来说，如果我们需要用到 <span class="math inline">\(\LaTeX\)</span> 这种复杂的系统来排版一份文档，那么该文档通常会是一篇有相当规模和专业性的作品，例如学术论文、技术报告、出版书籍等。这些作品中往往会包含封标题页、目录结构、章节层级、正文段落、插图、表格、参考文献等元素。这些元素通常都有着来自出版社、学术机构的，既定且严格的格式规范。接下来，我们将以一篇文档的实际编写过程为例来逐一介绍这些元素在 <span class="math inline">\(\LaTeX\)</span> 中的基本排版方式。</p>
<h3 id="章节层级设计">章节层级设计</h3>
<p>在正式开始撰写一篇文章之前，我们通常需要先拟定相应的写作大纲。在 <span class="math inline">\(\LaTeX\)</span> 的排版系统中，这项工作可以通过设计文档的章节层级来完成。当然，这需要根据我们在定义文档结构时所选择的排版类型来进行，因为它们使用到的命令是略有差异的。譬如在之前介绍的标准文档类中，<code>article</code>类文档的章节层级通常如下：</p>
<ul>
<li><strong>一级标题</strong>：由<code>\section</code>命令定义。</li>
<li><strong>二级标题</strong>：由<code>\subsection</code>命令定义。</li>
<li><strong>三级标题</strong>：由<code>\subsubsection</code>命令定义。</li>
<li><strong>四级标题（可选）</strong>：由<code>\paragraph</code>命令定义。</li>
</ul>
<p>而在<code>report</code>和<code>book</code>类的文档中，排版系统在上述命令之外还提供了级别更高的<code>\chapter</code>命令，和级别更低的<code>\subparagraph</code>命令，因此它们的章节层级一般包括：</p>
<ul>
<li><strong>一级标题</strong>：由<code>\chapter</code>命令定义。</li>
<li><strong>二级标题</strong>：由<code>\section</code>命令定义。</li>
<li><strong>三级标题</strong>：由<code>\subsection</code>命令定义。</li>
<li><strong>四级标题</strong>：由<code>\subsubsection</code>命令定义。</li>
<li><strong>五级标题（可选）</strong>：由<code>\paragraph</code>命令定义。</li>
<li><strong>六级标题（可选）</strong>：由<code>\subparagraph</code>命令定义。</li>
</ul>
<p><strong>请注意</strong>：在上述标准文档类中，<code>\paragraph</code>和<code>\subparagraph</code>这两个命令所显示的文本属于 run-in heading（即标题后紧跟着的正文），在默认情况下是不会自动换行的，所以它们更接近“带标题的段落”。并且，它需手动调整目录深度后才会被纳入目录。关于目录的生成，我们稍后会具体介绍。</p>
<p>下面，让我们以“<span class="math inline">\(\LaTeX\)</span> 排版示例”为题，来演示一下如何基于<code>article</code>类文档来设计章节层级，其具体代码如下：</p>
<pre><code class="language-tex">\documentclass[oneside, 12pt, a4paper]{article}

\usepackage{ctex}     % 用于提供中文支持
\usepackage{geometry} % 用于设置页面版式

% 设置页面边距
\geometry{a4paper, left=3cm, right=3cm, top=3cm, bottom=3cm}
% 设置文档元信息
\title{\LaTeX 排版示例}
\author{凌杰}
\date{\today}
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        \subsection{引言文本}
        \subsection{列表项目}
        \subsection{注释说明}
    \section{图表元素示例}
        \subsection{插图元素}
        \subsection{表格元素}
    \section{专业内容示例}
        \subsection{定理证明}
        \subsection{数学公式}
        \subsection{算法描述}
        \subsection{代码高亮}
\end{document}
</code></pre>
<p>而编译成 PDF 的输出效果如图2所示：</p>

<p><img alt="图2：章节层级示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122115853823-1686987064.png" class="lazyload"></p>
<p>在上述示例中，除了之前介绍过的文档结构定义部分之外，我们将用于组织文档内容的命令都放在了<code>\begin{document} ... \end{document}</code>这个环境定义内。在 <span class="math inline">\(\LaTeX\)</span> 语言中，文档内容的定义必须放在这个名为<code>document</code>的环境内，否则编译器会报错。这是因为<code>document</code>环境是 <span class="math inline">\(\LaTeX\)</span> 语言中用于定义文档内容的唯一环境，它告诉编译器从哪里开始处理文档内容。</p>
<p>而在<code>document</code>环境内，我们将文章分成了“正文样式示例”、“图表元素示例”和“专业内容示例”三个部分，每个部分都由一个一级标题（<code>\section</code>命令）来定义。每个一级标题下又分别定义了若干个二级标题（<code>\subsection</code>命令），它们共同组成了这篇文章的章节层级。</p>
<p>另外，如果我们在这里需要调整各章节的标题样式，可通过加载<code>titlesec</code>宏包。例如，通过下面这段代码，我们可以将使用<code>\section</code>命令定义的一级标题设置为大号粗体：</p>
<pre><code class="language-tex">% 如果是中文文档，建议先加载 ctex 宏包
\usepackage{titlesec}
\titleformat{\section}{\Large\bfseries}{\thesection}{1em}{}
% 在 \begin{document} ... \end{document} 之前添加上述代码
</code></pre>
<p><strong>请注意</strong>，我们在对专业文档进行排版时，务必要注意整体排版风格的一致性，原则上不宜随意混用多种风格，或进行过多的局部细节调整。尤其<code>titlesec</code>宏包与<code>ctex</code>、<code>hyperref</code>这些宏包在搭配处理中文标题时，可能还会出现一些意想不到的问题，更需谨慎使用。如果遇到非用不可的情况，建议先查阅相关文档，先搞清楚这些宏包的加载顺序，以及它们之间的相互影响，再进行尝试。</p>
<h3 id="正文内容排版">正文内容排版</h3>
<p>在确定了写作大纲之后，接下来就可以在这些既定的章节标题下面按部就班地填写正文内容了。下面，就让我们继续以“<span class="math inline">\(\LaTeX\)</span> 排版示例”这篇文章为例，来介绍如何使用 <span class="math inline">\(\LaTeX\)</span> 来排版它的正文内容。</p>
<h4 id="正文段落">正文段落</h4>
<p>在 <span class="math inline">\(\LaTeX\)</span> 语言中，普通的文本段落无需额外的命令来定义，直接将这些段落放置在相关章节的标题下面，各段落之间用空行分割即可。例如在上述文章中，如果我们想“正文段落”这个二级标题下面添加两个普通的正文段落，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        这是一段正文，它没有特殊的样式，我们只需直接写在章节标题下面即可。在这里继续添加文本，丰富一些内容以展示段落效果。
        
        这是另一段正文，它也没有特殊的样式，它与上一段正文之间用空行分割即可。在这里继续添加文本，丰富一些内容以展示段落效果。
        \subsection{引言文本}
        \subsection{列表项目}
        \subsection{注释说明}
    \section{图表元素示例}
        \subsection{插图元素}
        \subsection{表格元素}
    \section{专业内容示例}
        \subsection{定理证明}
        \subsection{数学公式}
        \subsection{算法描述}
        \subsection{代码高亮}
\end{document}
</code></pre>
<p>现在，如果我们重新编译这段代码，就可以看到“正文段落”这个二级标题下面已经出现了两个普通的正文段落，如图3所示：</p>

<p><img alt="图3：普通段落示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120125881-1176213538.png" class="lazyload"></p>
<p>值得一提的是，<span class="math inline">\(\LaTeX\)</span> 默认是不对每个章节下的第一个正文段落进行首行缩进的，但提供中文支持的<code>ctex</code>宏包更改了这一默认设置，因此在中文文档中，我们通常需要手动添加<code>\noindent</code>命令来取消首行缩进。例如，如果我们想取消上面第一段的首行缩进，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        \noindent 这是一段正文，它没有特殊的样式，我们只需直接写在章节标题下面即可。在这里继续添加文本，丰富一些内容以展示段落效果。

        这是另一段正文，它也没有特殊的样式，它与上一段正文之间用空行分割即可。在这里继续添加文本，丰富一些内容以展示段落效果。

%... 省略后续内容
\end{document}
</code></pre>
<p>另外，在普通的正文段落中，我们有时候也需要通过加粗、斜体、下划线等样式来突出某些重点内容，这些样式可以分别通过<code>\textbf{加粗强调}</code>、<code>\textit{斜体强调}</code>和<code>\underline{下划线强调}</code>这三个命令来实现。例如，如果我们想在上面的段落示例中添加一些强调效果，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        \noindent 这是一段正文，它没有特殊的样式，我们只需\textbf{直接写在章节标题下面}即可。在这里继续添加文本，丰富一些内容以展示段落效果。

        这是另一段正文，它也没有特殊的样式，它\textit{与上一段正文之间用空行分割}即可。在这里\underline{继续添加文本，丰富一些内容以展示段落效果}。

%... 省略后续内容
\end{document}
</code></pre>
<p>如果我们此刻再次编译这段代码，就可以看到上述段落中已经出现了加粗、斜体和下划线三种强调效果，如图4所示：</p>

<p><img alt="图4：强调效果示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120228356-1602383959.png" class="lazyload"></p>
<p>需要注意的是，<code>\underline</code>命令在中文换行时的表现并不好，所以最好避免使用它强调大段的中文文本。另外，在 <span class="math inline">\(\LaTeX\)</span> 排版系统中，斜体强调的语义与我们所熟悉的 Word 等文字处理软件中的斜体效果是不同的，它更接近于一种使用手写字体的强调效果，例如图4中，我们所看到的斜体强调效果实际上是中文字体由默认的宋体变为了楷体。如果是英文的话，基本上就是由罗马体变为了意大利体。</p>
<p>最后，如果我们想额外调整普通文本段落的行间距和段间距，就需要使用<code>\setlength</code>、<code>\linespread</code>命令来进行更为精细的设置。例如，如果我们想特别设置一下上述段落的行间距和段间距，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
\linespread{1.5}
\setlength{\parskip}{2em}
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        \noindent 这是一段正文，它没有特殊的样式，我们只需\textbf{直接写在章节标题下面}即可。在这里继续添加文本，丰富一些内容以展示段落效果。

        这是另一段正文，它也没有特殊的样式，它\textit{与上一段正文之间用空行分割}即可。在这里\underline{继续添加文本，丰富一些内容以展示段落效果。。

%... 省略后续内容
\end{document}
</code></pre>
<p>当然以上只是做个演示，我们在大多数情况下并不鼓励读者局部地去调整正文的行间距和段间距，因为 <span class="math inline">\(\LaTeX\)</span> 系统会自动根据文档内容来调整这些参数，以达到最佳的阅读效果。如果读者真的有这方面的需求，可以去查阅这两个命令或<code>setspace</code>宏包的详细文档，了解它们的用法和注意事项。</p>
<p>除了强调性的文本之外，我们有时还会需要在正文中设置一些超链接文本元素，以便读者可以快速跳转到一些指定的段落、图表或外部资源，这就需要通过加载<code>hyperref</code>宏包来实现。例如，如果我们想上述段落中插入一个能跳转到 TeX Live 官方网站的超链接，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
\usepackage{hyperref}
% 对超链接进行一些基本设置
\hypersetup{
    colorlinks=true,  % 将超链接文本设置为彩色
    linkcolor=blue,   % 将超链接文本的颜色设置为蓝色
    citecolor=blue,   % 将引用文本的颜色设置为蓝色
    urlcolor=blue,    % 将超链接地址的颜色设置为蓝色
    pdfauthor={凌杰}, % 设置 PDF 文档的作者信息
    pdftitle={\LaTeX 排版示例} % 设置 PDF 文档的标题信息
}
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        \noindent 这是一段正文，它没有特殊的样式，我们只需\textbf{直接写在章节标题下面}即可。在这里继续添加文本，丰富一些内容以展示段落效果。
        
        这是另一段正文，它也没有特殊的样式，它\textit{与上一段正文之间用空行分割}即可。在这里\underline{继续添加文本，丰富一些内容以展示段落效果}。
        
        我们还可以在正文中插入一个能跳转到 \href{https://www.tug.org/texlive/}{TeX Live 官方网站}的超链接。

%... 省略后续内容
\end{document}
</code></pre>
<p>如果我们此刻再次编译这段代码，就可以看到上述段落中已经出现了一个能跳转到 TeX Live 的超链接，如图5所示：</p>

<p><img alt="图5：超链接示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120324453-1328758940.png" class="lazyload"></p>
<p>需要注意的是，在 <span class="math inline">\(\LaTeX\)</span> 排版系统中，超链接文本在默认情况下会以红色方框的样式显示，这是<code>hyperref</code>宏包的标准行为。如果我们希望取消该默认样式并以彩色文字显示，就需要在全局设置中使用<code>\hypersetup</code>命令设置<code>colorlinks=true</code>选项。另外，超链接文本的默认颜色是红色，如果希望超链接文本能以其他颜色显示，就需要像我们在上面所做的那样，为其指定特定的颜色。</p>
<h4 id="引言文本">引言文本</h4>
<p>在写作过程中，我们经常会需要以引言的形式借用第三方的言论来增强文章的说服力。在 <span class="math inline">\(\LaTeX\)</span> 语言中，我们可以通过定义一个名为<code>quote</code>环境来设置引言形式的文本。这种形式的文本会自动呈现出相对于其他文本进行左右缩进的排版效果。例如，如果我们想在上述文章中的“引言文本”这个二级标题下面添加一段引言，并以右对齐的方式声明引言的出处，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        %... 省略之前的正文段落内容
        \subsection{引言文本}
            \noindent 下面将显示的是一段引言文本的示例：
            \begin{quote}
                \textit{“\LaTeX 是一种排版系统，它使用TeX排版引擎，并遵循一套文档类和宏包的规范，以实现文档的自动化排版。”}
        
                {\raggedleft -- 高德纳（Donald E. Knuth） \par}
            \end{quote}
    %... 省略后续内容
\end{document}
</code></pre>
<p>在上述示例中，我们首先使用了<code>\begin{quote} ... \end{quote}</code>这个环境定义设置了一段引言文本。在<code>quote</code>环境内部，我们可以使用普通的文本命令（如<code>\textit{}</code>、<code>\underline{}</code>等）来设置这段文本的样式。接下来，我们用<code>\raggedleft</code>命令将引言的来源设置成了右对齐的文本，与引言正文保持一定的间距（这里使用<code>\hfill</code>命令也可以实现相同的效果）。最后，我们使用<code>\par</code>命令结束了<code>\raggedleft</code>命令的使用效果，以防它的排版效果意外影响到其他文本。</p>
<p>当然，<code>quote</code>环境通常只用于设置单一段落的引言文本，如果需要设置包含多个自然段的较长文本，<code>quotation</code>环境可能是更合适的选择。例如，如果我们想继续在之前的引言后面再引用来自维基百科的两段文本，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        \subsection{正文段落}
        % 省略之前的正文段落内容
        \subsection{引言文本}
        % 省略之前的引言文本内容

        \noindent 下面是一个较长的引用文本示例：
        \begin{quotation}
            \textit{
                “Leslie Lamport 在 1980 年代初期开发了 LaTeX 系统，以简化 TeX 的使用。LaTeX 提供了一套宏和命令，使用户能够专注于文档内容，而无需过多关注排版细节。LaTeX 迅速成为学术界和技术文档撰写的标准工具，尤其在数学、计算机科学等领域得到了广泛应用。
                
                LaTeX 的设计理念强调结构化文档和内容优先，这使得它在处理复杂文档时表现出色。通过使用各种宏包，用户可以轻松地扩展 LaTeX 的功能，以满足不同的排版需求。今天，LaTeX 已经发展成为一个庞大的生态系统，拥有丰富的资源和活跃的社区支持。”
            }
            {\raggedleft -- 维基百科（Wikipedia） \par}
        \end{quotation}
    %... 省略后续内容
\end{document}
</code></pre>
<p>现在，如果我们再次编译这个文档，就可以看到“引言文本”这个二级标题下面已经出现了上面所设置的那两个引言单元，如图6所示：</p>

<p><img alt="图6：引言文本示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120418813-723165418.png" class="lazyload"></p>
<p>除此之外，如果读者还想设置排版更为专业的引言文本，可能就需要通过加载<code>csquotes</code>、<code>epigraph</code>这类第三方提供的宏包来实现了。关于这方面的操作，读者可参照它们的文档说明来进行。我们在这里就不再继续展开了。</p>
<h4 id="列表元素">列表元素</h4>
<p>在写作过程中，我们有时会需要以列表的形式展示一些有序或无序的选项、步骤或要点内容。在 <span class="math inline">\(\LaTeX\)</span> 语言中，我们可以分别通过<code>itemize</code>、<code>enumerate</code>、<code>description</code>这三个环境来设置相应的列表元素。例如，如果我们想在上述文章中的“列表元素”这个二级标题下面添加一个有序列表、一个无序列表以及一个描述列表，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        % 省略之前的章节
        \subsection{列表项目}
            \noindent 下面是一个有序列表的示例：
            \begin{enumerate}
                \item 第一个选项
                \item 第二个选项
                \item 第三个选项
            \end{enumerate}

            \noindent 下面是一个无序列表的示例：
            \begin{itemize}
                \item 第一个选项
                \item 第二个选项
                \item 第三个选项
            \end{itemize}

            \noindent 下面是一个描述列表的示例：
            \begin{description}
                \item[第一个选项] 这是第一个选项的描述内容
                \item[第二个选项] 这是第二个选项的描述内容
                \item[第三个选项] 这是第三个选项的描述内容
            \end{description}
    %... 省略后续内容
\end{document}
</code></pre>
<p>在上述示例中，我们分别使用了<code>\begin{enumerate} ... \end{enumerate}</code>、<code>\begin{itemize} ... \end{itemize}</code>、<code>\begin{description} ... \end{description}</code>这三个环境来设置有序列表、无序列表以及描述列表。在列表内部，我们可以使用<code>\item</code>命令来定义每个列表项的内容。需要注意的是，在<code>description</code>环境中，每个列表项的内容前面会自动加上一个描述标签，我们可以通过<code>\item[标签名]</code>命令来设置这个标签的名称。</p>
<p>如果读者还想设置更为复杂的多层次列表，就需要通过嵌套定义另一个名为<code>enumerate</code>的环境来实现了。该环境在默认情况下最多允许我们嵌套定义四层列表，<span class="math inline">\(\LaTeX\)</span> 会自动为每个列表项添加相应的编号。例如，如果我们想在上述文章中接着上面的描述列表添加一个三层嵌套的多层次列表，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        % 省略之前的章节
        \subsection{列表项目}
            % 省略之前的列表内容

            \noindent 下面是一个多层次列表的示例：
            \begin{enumerate}
                \item 第一个选项
                \item 第二个选项
                \item \begin{enumerate}
                    \item 第一个选项
                    \item 第二个选项
                    \item \begin{enumerate}
                        \item 第一个选项
                        \item 第二个选项
                        \item 第三个选项
                    \end{enumerate}
                \end{enumerate}
            \end{enumerate}
    %... 省略后续内容
\end{document}
</code></pre>
<p>如你所见，我们在上述示例中选套定义了四个<code>enumerate</code>环境，分别对应三层嵌套的多层次列表。在默认情况下，<span class="math inline">\(\LaTeX\)</span> 会为每个列表项自动添加编号，并按照嵌套的层级关系递增。</p>
<p>现在，如果我们再次编译这个文档，就可以看到“列表项目”这个二级标题下面已经出现了四种不同的列表单元，如图7所示：</p>

<p><img alt="图7：列表元素示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120602248-325052290.png" class="lazyload"></p>
<h4 id="注释说明">注释说明</h4>
<p>在写作过程中，我们有时还会需要对特定的内容添加一些注释说明，以帮助读者更好地理解文档内容。在 <span class="math inline">\(\LaTeX\)</span> 语言中，我们可以通过<code>\footnote{}</code>命令来设置脚注，或者通过<code>\marginpar{}</code>命令来设置边注。例如，如果我们想在上述文章中的“注释说明”这个二级标题下面分别添加一段被脚注和边注的文本，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
\marginparwidth=2cm  % 设置边注的宽度
\marginparsep=0.2cm  % 设置边注与正文的间距

% 开始组织文档内容
\begin{document}
    \section{正文样式示例}
        % 省略之前的章节
        \subsection{注释说明}
            \noindent 这是一段要被脚注的文本。
            \footnote{这是脚注的内容。}

            \noindent 这是一段要被边注的文本。
            \marginpar{这是右侧边注内容。}
    %... 省略后续内容
\end{document}
</code></pre>
<p>在上述示例中，我们首先使用<code>\footnote{}</code>命令设置了一段脚注内容。在脚注内部，我们可以使用普通的文本命令（如<code>\textit{}</code>、<code>\underline{}</code>等）来设置这段文本的样式。接下来，我们使用<code>\marginpar{}</code>命令设置了一段边注内容。在边注内部，我们同样可以使用普通的文本命令来设置这段文本的样式。需要注意的是，在设置边注之前，我们需要先在文档的全局设置区使用<code>\marginparwidth</code>命令来设置边注的宽度，并通过<code>\marginparsep</code>命令来设置边注与正文的间距。</p>
<p>现在，如果我们再次编译这个文档，就可以看到“注释说明”这个二级标题下面已经出现了上面所设置的脚注和边注，如图8所示：</p>

<p><img alt="图8：注释说明示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120746268-813579506.png" class="lazyload"></p>
<p>需要提醒读者的是，在加载了<code>ctex</code>宏包的中文文档中，<code>\marginpar</code>命令所设置边注的具体位置可能受版面布局影响而出现偏移；如需更稳定的边注布局，使用<code>marginnote</code>宏包会个更好的选择。关于这方面的操作，读者可参照<code>marginnote</code>宏包的文档说明来进行。我们在这里就不再继续展开了。</p>
<h2 id="标题页与目录">标题页与目录</h2>
<p>在 <span class="math inline">\(\LaTeX\)</span> 排版系统中，完成针对文档内容的排版后，我们通常会为其添加一个标题页（也可以理解为封面），并根据它的章节层级为其自动生成一个目录。其中，用于生成标题页的命令为<code>\maketitle</code>，而生成目录的命令则是<code>\tableofcontents</code>。例如，如果我们想在上述文章中添加一个标题页和一个目录，就可以像下面这样写：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
% 开始组织文档内容
\begin{document}
    \maketitle        % 添加标题页
    \tableofcontents  % 添加目录
    \newpage          % 添加分页符
    \section{正文样式示例}
        % 省略之前的章节
        \subsection{注释说明}
            % 省略之前的注释说明
    %... 省略后续内容
\end{document}
</code></pre>
<p>如你所见，我们在<code>\begin{document}</code>命令之后，章节层级的定义之前的位置上添加了三条命令。首先，<code>\maketitle</code>命令用于自动根据我们之前设置的文档元信息来生成标题页。接下来，<code>\tableofcontents</code>命令则会根据<code>\section{}</code>、<code>\subsection{}</code>等命令所定义的章节层级来自动生成文档的目录。最后，<code>\newpage</code>命令用于在目录与正文之间添加一个分页符，以便让正文内容从新的一页开始。</p>
<p>另外在默认情况下，目录所涉及的章节层级只有三级，如果读者想涉及到更多或更少的章节层级，就需要在文档的全局设置区使用<code>\setcounter{tocdepth}{层级数}</code>命令来设置目录的层级深度，例如像下面这样：</p>
<pre><code class="language-tex">% 省略之前的文档结构定义部分
\setcounter{tocdepth}{2}  % 设置目录的层级深度

% 开始组织文档内容
\begin{document}
    \maketitle  % 添加标题页
    \tableofcontents  % 添加目录
    \section{正文样式示例}
        % 省略之前的章节
        \subsection{注释说明}
            % 省略之前的注释说明
    %... 省略后续内容
\end{document}
</code></pre>
<p>在上述示例中，我们使用<code>\setcounter{tocdepth}{4}</code>命令将目录的层级深度设置为4级。现在，如果我们再次编译这个文档，就可以看到“正文样式示例”这个一级标题下面已经出现了上面所设置的标题页和目录，如图9所示：</p>

<p><img alt="图9：标题页与目录示例" loading="lazy" data-src="https://img2024.cnblogs.com/blog/691082/202511/691082-20251122120839461-833604481.png" class="lazyload"></p>
<h2 id="笔记小结">笔记小结</h2>
<p>在本篇笔记中，我们主要介绍了 <span class="math inline">\(\LaTeX\)</span> 语言中的一些基本排版功能，包括文档的排版结构、标题样式、正文样式、列表元素、注释说明、标题页与目录。通过这些排版功能，我们可以方便地设置出一篇纯文本文档的排版结构、样式以及内容。当然，针对更为专业的科普、学术文档，<span class="math inline">\(\LaTeX\)</span> 系统还提供了许多其他高级排版功能，如表格、图片、公式等。在后续的笔记中，我们将继续介绍这些高级排版功能。</p>

</div>
<div class="clear"></div>

        <p class="postfoot">
            posted on 
<span id="post-date" data-last-update-days="0" data-date-updated="2025-11-22 12:11">2025-11-22 12:10</span>&nbsp;
<a href="https://www.cnblogs.com/owlman">凌杰</a>&nbsp;
阅读(<span id="post_view_count">0</span>)&nbsp;
评论(<span id="post_comment_count">0</span>)&nbsp;
&nbsp;
<a href="javascript:void(0)" onclick="AddToWz(19256216);return false;">收藏</a>&nbsp;
<a href="javascript:void(0)" onclick="reportManager.report({ currentUserId: '', targetType: 'blogPost', targetId: '19256216', targetLink: 'https://www.cnblogs.com/owlman/p/19256216', title: 'LaTeX学习笔记：文档排版基础' })">举报</a>

        </p>
    