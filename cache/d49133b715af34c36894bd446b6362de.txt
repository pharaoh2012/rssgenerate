
            <h1 class="postTitle">
                <a id="cb_post_title_url" class="postTitle2 vertical-middle" href="https://www.cnblogs.com/szdytom/p/19822211/tee-over-supply-chain" title="发布于 2026-04-05 01:07">
    <span role="heading" aria-level="2">用硬件隔离阻断供应链“病毒式”投毒：TEE 能做什么，不能做什么？</span>
    

</a>

            </h1>
            <div class="clear"></div>
            <div class="postBody">
                <div id="cnblogs_post_body" class="blogpost-body cnblogs-markdown">
<p>（使用AI整理内容并编排格式）</p>
<blockquote>
<p>近期 Axios、Apifox、LiteLLM 等事件集中爆发，暴露了一个深层问题：我们以为信任的是开源代码，实际上信任的是平台和开发者。TEE 技术或许能打破这个困局。</p>
</blockquote>
<h2 id="从病毒式传播看供应链投毒的本质">从“病毒式传播”看供应链投毒的本质</h2>
<p>2026 年开春以来，供应链攻击几乎以“周更”的速度冲击开发者社区：</p>
<ul>
<li><strong>Axios</strong>：核心维护者 NPM 账户被社工 → 发布两个恶意版本 → 通过 postinstall 钩子部署 RAT。</li>
<li><strong>Apifox</strong>：CDN 被篡改 → Electron 客户端加载恶意 JS → 窃取凭证、执行系统命令。</li>
<li><strong>LiteLLM</strong>：CI 流水线令牌泄露 → PyPI 后门版本 → 利用 <code>.pth</code> 文件持久化。</li>
</ul>
<p>仔细看这些事件，它们不是孤立的。<strong>攻击具有明显的“病毒式传播”特征</strong>：</p>
<ol>
<li>早期利用脆弱的 LLM Agent 或自动化工具（如漏洞百出的 OpenClaw 等）批量盗取开发者账户。</li>
<li>盗取到的账户中，部分属于热门包的维护者 → 用这些账户发布带后门的包。</li>
<li>后门包一旦被安装，立刻窃取更多机器的 NPM / PyPI / GitHub 令牌 → 扩大感染面。</li>
<li>循环往复，形成自我增殖的供应链蠕虫。</li>
</ol>
<p><strong>根本问题不是某个漏洞，而是整个信任模型的错位。</strong></p>
<h2 id="我们真正信任的是什么">我们真正信任的是什么？</h2>
<p>社区常说“开源代码经得起社区审查，所以值得信任”。但现实是：</p>
<ul>
<li>你安装一个 npm 包时，并没有验证它是否真的来自公开的源码仓库。</li>
<li>你信任的是 NPM 平台没有作恶、开发者密钥没泄露、CI 环境没被入侵。</li>
<li>换句话说，你信任的是<strong>人和平台，而不是代码本身</strong>。</li>
</ul>
<p>这种信任在单点被突破后就会彻底崩塌。我们需要一种<strong>不依赖平台和开发者诚信</strong>的验证方式。</p>
<h2 id="整体思路tee-作为可信构建锚点">整体思路：TEE 作为“可信构建锚点”</h2>
<p><strong>可信执行环境（TEE）</strong>，尤其是云端的 VM 级 TEE（Intel TDX / AMD SEV‑SNP），提供了一种硬件隔离的“安全黑盒”：</p>
<ul>
<li>在 TEE 中构建软件包，外界（包括云服务商、操作系统、CI 系统）无法读取或篡改内部执行。</li>
<li>构建完成后，TEE 生成一个<strong>加密的 attestation 证明</strong>，内容包含：
<ul>
<li>构建产物的哈希（如 tarball、二进制文件）</li>
<li>源代码仓库的 commit 哈希</li>
<li>构建环境指纹（OS、编译器、依赖版本等，如 docker 的 digest 信息）</li>
</ul>
</li>
<li>任何人都可以验证这个证明，<strong>无需信任构建者</strong>，只需信任 CPU 硬件和云服务商的根证书。</li>
</ul>
<div class="mermaid">sequenceDiagram
    participant Dev as 开发者
    participant Git as 源码仓库 (Git)
    participant CI as CI 系统 (含 TEE 环境)
    participant TEE as TEE 实例 (TDX/SEV-SNP)
    participant Registry as 包仓库 (npm/PyPI)
    participant Client as 客户端 (包管理器)

    Dev-&gt;&gt;Git: 1. 推送代码 + 打 tag
    Git-&gt;&gt;CI: 2. 触发构建流水线
    CI-&gt;&gt;TEE: 3. 启动 TEE 实例 (Attestable Builds)
    Note over TEE: 硬件隔离环境&lt;br/&gt;外界无法读取/篡改
    TEE-&gt;&gt;Git: 4. 拉取指定 commit 源码
    TEE-&gt;&gt;TEE: 5. 执行构建脚本 (编译/打包)
    TEE-&gt;&gt;TEE: 6. 计算产物哈希 + 记录环境指纹
    TEE-&gt;&gt;TEE: 7. 生成 Attestation 证明&lt;br/&gt;(产物哈希, commit, 环境, 时间戳)
    TEE-&gt;&gt;TEE: 8. 用 CPU 硬件私钥签名证明
    TEE-&gt;&gt;CI: 9. 返回构建产物 + 签名后的证明
    CI-&gt;&gt;Registry: 10. 发布包 + 附带证明 (attestation)
    Client-&gt;&gt;Registry: 11. 请求下载包
    Registry-&gt;&gt;Client: 12. 返回包 + 证明
    Client-&gt;&gt;Client: 13. 验证证明签名 (信任 CPU 根证书)
    Client-&gt;&gt;Client: 14. 核对产物哈希是否匹配&lt;br/&gt;commit 是否公开可审计
    alt 验证通过
        Client-&gt;&gt;Client: 15. 安装/使用包
    else 验证失败
        Client-&gt;&gt;Client: 15. 拒绝安装，告警
    end
</div><p><strong>应用到供应链</strong>：</p>
<ul>
<li>要求所有包（npm、PyPI、容器镜像等）必须附带 TEE attestation。</li>
<li>客户端或包管理器在安装前验证证明：代码是否真的来自公开的源码 commit，环境是否匹配。</li>
<li>攻击者即使盗用账户、控制 CI、甚至黑掉 npm 服务器，也无法伪造一个合法的 attestation（因为需要硬件签名）。</li>
</ul>
<h2 id="与现有方法的对比">与现有方法的对比</h2>
<table>
<thead>
<tr>
<th>方法</th>
<th>优点</th>
<th>缺点</th>
<th>TEE 相比之下的优势</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>包签名（如 Sigstore）</strong></td>
<td>生态成熟，验证简单</td>
<td>信任开发者密钥；密钥泄露则失效</td>
<td>不依赖密钥，依赖硬件；泄露密钥无法伪造证明</td>
</tr>
<tr>
<td><strong>可重现构建（Reproducible Builds）</strong></td>
<td>可独立验证源码→产物</td>
<td>需多方独立构建，现实中少有人做；无法防御构建环境被篡改</td>
<td>单方 TEE 即可证明环境未被篡改，且比多方构建便宜</td>
</tr>
<tr>
<td><strong>SLSA 框架</strong></td>
<td>定义清晰的构建完整性级别</td>
<td>高等级（L3/L4）依赖平台隔离，但平台本身仍可被入侵</td>
<td>TEE 提供硬件级隔离，可达到 SLSA L4 且不信任平台</td>
</tr>
</tbody>
</table>
<p><strong>TEE 自己的缺点</strong>：</p>
<ul>
<li>生态几乎为零：需要包管理器、CI、客户端全部改造。</li>
<li>硬件依赖：必须使用支持 TDX/SEV‑SNP 的云实例或本地 CPU。</li>
<li>成本：每构建成本较高，对海量小包累计成本可观。</li>
<li>不能防御源码级后门（XZ Utils 类攻击）。</li>
</ul>
<h2 id="对近期事件的简要分析">对近期事件的简要分析</h2>
<h3 id="-litellmci-令牌泄露--发布后门">✅ LiteLLM（CI 令牌泄露 → 发布后门）</h3>
<p><strong>TEE 能完全阻止</strong>：CI 构建运行在 TEE 内，发布令牌只对特定度量的 TEE 释放。攻击者即使控制宿主机，也无法窃取令牌或篡改构建产物。</p>
<h3 id="-axiosnpm-账户被盗--发布恶意包">✅ Axios（NPM 账户被盗 → 发布恶意包）</h3>
<p><strong>TEE 能阻止</strong>：NPM 要求附带 attestation。攻击者可以发布，但无法生成合法证明（因为源码仓库未被篡改）。客户端拒绝安装无证明的包。</p>
<h3 id="️-apifox闭源-electroncdn-篡改">⚠️ Apifox（闭源 Electron，CDN 篡改）</h3>
<p><strong>TEE 部分有效</strong>：如果官方使用 TEE 构建并签名，客户端可验证签名防止 CDN 篡改。但闭源意味着用户无法独立验证“源码是否恶意”，以及无法防御官方的密钥泄露，最终仍需信任官方。TEE 不能解决闭源软件的“作恶”问题。但是至少可以防止代码在构建、分发的流程中受到攻击。</p>
<h3 id="-xz-utils社会工程学植入源码后门">❌ XZ Utils（社会工程学植入源码后门）</h3>
<p><strong>TEE 无能为力</strong>：后门在源代码中，TEE 会忠实地将其构建进产物并出具合法证明。要防御此类攻击，需要<strong>更强的代码审查流程</strong>。</p>
<h2 id="实践考量">实践考量</h2>
<p>CCS 2025 的论文 <a href="https://arxiv.org/abs/2505.02521" target="_blank" rel="noopener nofollow">Attestable Builds: Compiling Verifiable Binaries on Untrusted Systems using Trusted Execution Environments</a> 已经提出了一套相当完整的 TEE 构建系统设计，我们刚刚考虑的“可证明构建”（Attestable Builds, A-Bs）这一新范式已经在该文献中得到了详细的研究。该文献利用现代可信执行环境（如 AWS Nitro Enclaves、AMD SEV-SNP）的完整性保护与远程证明能力，结合嵌套沙箱（如 gVisor），在不可信的云构建服务中确保二进制确由指定源码快照生成。与传统的可重现构建（Reproducible Builds）相比，A-Bs 无需修改项目源码和构建脚本，仅需少量配置变更即可接入现有 CI/CD 流水线（如 GitHub Actions），并可提供近乎即时的验证，显著降低了工程门槛和持续维护成本。</p>
<p>论文进一步给出了完整的架构设计、威胁模型和基于 Tamarin 的形式化验证，证明其能抵御构建服务器篡改、跨租户攻击、仓库欺骗等多种威胁。原型实现评估显示，A-Bs 的启动开销约 42 秒，默认配置下构建时长增加约 14%，且能成功编译 LLVM Clang、Linux 内核等复杂项目以及多个难以重现的 Debian 软件包。作者还讨论了 A-Bs 与 R-Bs 的组合方案，可实现“任意信任”（any-trust）模型，以及将证明扩展到测试结果、静态分析等非二进制产物的前景。整体而言，该工作为在不可信环境中建立可验证的构建流程提供了务实且可落地的解决方案。</p>
<p>从现实角度来看，虽然 TEE 的生态尚未成熟，但其提供的“硬件级信任锚”特性确实能在根本上改变供应链安全的信任模型。在短期内，可能需要先在关键组件（如核心库、基础镜像）上试点，逐步推广到整个生态。同时，考虑到成本问题，也许可以采用分层策略：对于高下载量，被依赖广泛，风险较高（如有 postinstall 钩子）的包强制使用 TEE 证明，而对于小众包则允许传统签名或无证明的方式。</p>
<p>有关这种面向实践的分级策略的设计和评估，以及对前文中我们提出的供应链蠕虫的循环传播模型的建模分析，可能是未来研究的一个有趣方向。如果能够用真实 npm/PyPI 下载数据模拟攻击者的传播路径，找到分级策略的在成本曲线和攻击阻断率之间的最佳平衡点，将对推动 TEE 在供应链安全中的实际应用具有重要意义。</p>
<h2 id="小结">小结</h2>
<p>供应链投毒的“病毒式传播”之所以可怕，是因为攻击者一旦突破一个信任点（密钥、CI、平台），就能快速扩散。TEE 将信任从“人+平台”迁移到“硬件+公开代码”，<strong>切断了这种传播链</strong>。它不能替代代码审查，也不能解决社会工程学。但至少，当我们说“这个包来自开源代码”时，TEE 能让我们<strong>真正验证这一点</strong>，而不是仅仅相信某个平台上的某个开发者。</p>
<p>在这一方面，虽然已经有了理论上的方案和初步的原型实现，但要真正落地，还需要社区、平台和开发者的共同努力。未来的研究可以进一步探索如何在实际生态中推广 TEE 证明，如何设计合理的分级策略，以及如何结合其他安全措施（如代码审查、行为分析）形成多层防御体系。</p>


</div>
<div class="clear"></div>

            </div>
            <div class="postDesc">posted @ 
<span id="post-date" data-last-update-days="0.40347222222222223" data-date-updated="2026-04-05 10:48">2026-04-05 01:07</span>&nbsp;
<a href="https://www.cnblogs.com/szdytom">方而静</a>&nbsp;
阅读(<span id="post_view_count">77</span>)&nbsp;
评论(<span id="post_comment_count">0</span>)&nbsp;
&nbsp;
<a href="javascript:void(0)" onclick="AddToWz(19822211);return false;">收藏</a>&nbsp;
<a href="javascript:void(0)" onclick="reportManager.report({ currentUserId: '', targetType: 'blogPost', targetId: '19822211', targetLink: 'https://www.cnblogs.com/szdytom/p/19822211/tee-over-supply-chain', title: '用硬件隔离阻断供应链“病毒式”投毒：TEE 能做什么，不能做什么？' })">举报</a>
</div>
        