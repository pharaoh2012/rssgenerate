
            <h1 class="postTitle">
                <a id="cb_post_title_url" class="postTitle2 vertical-middle" href="https://www.cnblogs.com/charlee44/p/19609813" title="发布于 2026-02-12 21:41">
    <span role="heading" aria-level="2">从零实现一个生产级 RAG 语义搜索系统：C++ + ONNX + FAISS 实战</span>
    

</a>

            </h1>
            <div class="clear"></div>
            <div class="postBody">
                    <div id="cnblogs_post_description" style="display: none">
        
        本文从零构建了一个轻量级、高性能的 C++ 语义搜索系统，基于 ONNX 运行 BGE 嵌入模型、FAISS 向量索引与 Markdown 语义分块，完整实现支持增删改查的生产级 RAG 检索后端。
    </div>
<div id="cnblogs_post_body" class="blogpost-body cnblogs-markdown">
<h1 id="1-引言">1. 引言</h1>
<p>既然是“从零实现”，本文暂不深入探讨繁复的理论背景，而是先聚焦一个核心问题：语义化搜索中的“语义化”到底是什么意思？</p>
<p>传统的关键词搜索依赖字面匹配——比如搜索“如何序列化 JSON”，系统只会返回包含这些精确关键词的文档。这种方式简单直接，但十分“呆板”：它无法理解“JSON 序列化方法”或“把对象转成 JSON 字符串”其实表达了相同的意图。</p>
<p>而语义化搜索的突破在于：它不再比较文字，而是比较含义。其核心思想是将文本（无论是查询还是文档）通过嵌入模型（Embedding Model）转换为高维向量——这个过程称为“向量化”或“嵌入”（Embedding）。在向量空间中，语义相近的句子会被映射到彼此靠近的位置。于是，搜索就变成了一个向量相似度计算问题：找出与查询向量最接近的文档向量。</p>
<p>表面上看，这是数学（向量、内积、距离）；本质上，这是智能——因为模型通过海量数据学习到了人类语言的语义结构。而我们要做的，就是用编程语言把这套“用数学理解语言”的能力，变成一个高效、可靠、可部署的生产级系统。</p>
<h1 id="2-目标">2. 目标</h1>
<p>本文的初衷，是为我的个人博客网站 <a href="https://charlee44.com/" target="_blank" rel="noopener nofollow"><strong>Charlee44 的技术驿站</strong></a> 实现一套真正可用的站内搜索功能。由于博客部署在资源极其有限的云服务器上（比如1核 CPU、1GB 内存），我对系统提出了两个“极致”要求：<strong>极致的性能</strong> 与 <strong>极致的资源效率</strong>。</p>
<p><img alt="个人博客网站的站内搜索功能" loading="lazy" data-src="https://img2024.cnblogs.com/blog/1000410/202602/1000410-20260212213951954-1452703526.png" class="lazyload"></p>
<p>毕竟，每一分算力都来自自己的钱包——这促使我放弃了主流但相对“重”的 Python 生态（如 Sentence Transformers + ChromaDB 的常规组合），转而选择 <strong>C++</strong> 作为实现语言。C++ 不仅能提供更低的内存开销和更高的推理吞吐，也让我有机会深入理解 embedding 推理、向量索引、文本分块等模块的底层机制。</p>
<p>当然，需要说明的是：<strong>如果是在企业环境中开发，或对迭代速度要求更高，Python 生态仍是更高效、更成熟的选择</strong>。而本项目更多是一次“用工程约束驱动深度学习”的实践——在有限资源下，亲手构建一个轻量、可控、可落地的语义搜索系统。</p>
<h1 id="3-嵌入模型">3. 嵌入模型</h1>
<p>既然语义化搜索的核心在于将文本转化为富含语义的向量，那么实现这一能力的关键，就在于选择一个合适的嵌入模型。这类模型的作用，是将任意长度的文本映射为固定维度的稠密向量，使得语义相似的文本在向量空间中距离更近。笔者这里使用的是 <a href="https://huggingface.co/BAAI/bge-small-zh-v1.5" target="_blank" rel="noopener nofollow">bge-small-zh-v1.5</a> 。bge-small-zh-v1.5 是由北京智源人工智能研究院（BAAI）推出的 BGE（BAAI General Embedding）系列中的轻量级中文模型。它基于 Transformer 架构，在大规模中英文语料上进行对比学习训练，专为检索任务优化。尽管它并非该系列中最新或最大的版本（如 bge-large），但其在推理速度、内存占用与语义质量之间取得了极佳的平衡，尤其适合资源受限或对延迟敏感的生产环境。</p>
<p>bge-small-zh-v1.5 可以从 Hugging Face 上下载，下载后的数据文件组织结构如下：</p>
<pre><code class="language-text">bge-small-zh-v1.5/
├── config.json                     # 模型架构配置（层数、隐藏层维度等）
├── pytorch_model.bin               # PyTorch 格式的模型权重（核心文件）
├── tokenizer.json                  # 分词器定义（WordPiece 词汇表 + 算法参数）
├── tokenizer_config.json           # 分词器配置（如是否小写化、特殊 token 等）
├── vocab.txt                       # WordPiece 词汇表（纯文本，每行一个 token）
├── special_tokens_map.json         # 特殊 token 映射（[CLS], [SEP], [PAD] 等）
├── modules.json                    # （可选）模型模块信息
├── sentence_bert_config.json       # （可选）Sentence-BERT 相关配置
├── README.md                       # 模型卡片（含使用说明、引用信息等）
└── .gitattributes                  # Git LFS 配置（用于大文件管理）
</code></pre>
<p>不过，bge-small-zh-v1.5 原生基于 PyTorch（属于 Python 生态），直接在 C++ 环境中调用并不方便，也难以满足我们对性能和资源占用的要求。为了在 C++ 中高效运行该模型，最佳实践是将其导出为 ONNX 格式。</p>
<p>ONNX（Open Neural Network Exchange）是一种开放的神经网络模型交换格式，由微软、Facebook 等公司共同推动，旨在实现“一次训练，多端部署”。它不依赖特定框架（如 PyTorch 或 TensorFlow），而是将模型结构与权重统一序列化为标准格式，从而可在不同硬件和语言环境中高效推理。</p>
<p>要在 C++ 中加载和运行 ONNX 模型，我们需要使用 ONNX Runtime——这是微软开源的高性能推理引擎，支持 CPU/GPU 加速、跨平台（Windows/Linux/macOS）以及 C/C++/Python 等多种语言绑定。通过 ONNX Runtime，我们可以在无 Python 依赖的情况下，以极低的开销完成 embedding 推理，完美契合本项目的轻量级目标。</p>
<p>因此，关键的第一步是需要将 bge-small-zh-v1.5 转换成 ONNX 格式的嵌入模型。嵌入模型的格式转换是预处理步骤，可以通过 Python 脚本来实现：</p>
<pre><code class="language-python"># export_onnx.py
from transformers import AutoTokenizer, AutoModel
from optimum.onnxruntime import ORTModelForFeatureExtraction
from pathlib import Path

# 改为你的本地模型路径（注意：使用原始字符串或正斜杠）
model_path = "C:/Github/search/bge-small-zh-v1.5" 
onnx_path = "./bge-small-zh-onnx"

# 导出 ONNX（从本地模型加载）
ort_model = ORTModelForFeatureExtraction.from_pretrained(
    model_path,           # ← 关键：使用本地路径
    export=True,
    provider="CPUExecutionProvider"
)

tokenizer = AutoTokenizer.from_pretrained(model_path)  # ← 同样用本地路径

# 保存 ONNX 模型和 tokenizer
ort_model.save_pretrained(onnx_path)
tokenizer.save_pretrained(onnx_path)

print(f"✅ ONNX 模型已导出到 {onnx_path}")
</code></pre>
<p>转换后的 ONNX 格式嵌入模型 bge-small-zh-onnx 的数据文件组织结构如下：</p>
<pre><code class="language-python">bge-small-zh-onnx/
├── model.onnx                      # ✅ 核心：ONNX 格式的模型计算图（含权重）
├── config.json                     # 模型架构配置（与原版一致）
├── tokenizer.json                  # 分词器定义（WordPiece + 预处理规则）
├── tokenizer_config.json           # 分词器行为配置（如 do_lower_case）
├── vocab.txt                       # WordPiece 词汇表（纯文本备份）
└── special_tokens_map.json         # 特殊 token 映射（[CLS], [SEP], [PAD] 等）
</code></pre>
<h1 id="4-分词器">4. 分词器</h1>
<p>在使用 ONNX Runtime 加载嵌入模型对文本进行向量化之前，我们还需要了解一个关键组件：<strong>分词器</strong>（Tokenizer）。</p>
<p>所谓“字、词、句、段、篇”，语言的理解是分层的。但对于现代深度学习模型（尤其是基于 Transformer 的架构）而言，它们并不直接处理原始字符，而是将文本<strong>切分为更小的语义单元——“词元”</strong>（token）。这个过程就是 <strong>Tokenization</strong>（词元化），由分词器完成。</p>
<p>分词器的重要性体现在以下两点：</p>
<ol>
<li><strong>模型输入的前提</strong>：嵌入模型只接受 token ID 序列作为输入，而非原始字符串。没有正确的分词，就无法生成有效的 embedding。</li>
<li><strong>影响语义精度</strong>：不同的分词策略会导致不同的 token 序列，进而影响向量表达的质量。</li>
</ol>
<h2 id="41-自定义分词器">4.1 自定义分词器</h2>
<p>其实分词器的实现也并不神秘，我们完全可以实现一个简单版本的：</p>
<pre><code class="language-cpp">#include &lt;fstream&gt;
#include &lt;string&gt;
#include &lt;unordered_map&gt;
#include &lt;vector&gt;

#ifdef _WIN32
#include &lt;Windows.h&gt;
#endif

using namespace std;

std::unordered_map&lt;std::string, int&gt; LoadVocab(const std::string&amp; path) {
  std::unordered_map&lt;std::string, int&gt; vocab;
  std::ifstream file(path);
  std::string token;
  int id = 0;
  while (std::getline(file, token)) {
    vocab[token] = id++;
  }
  return vocab;
}

// 注意：需处理 UTF-8 多字节字符！
std::vector&lt;std::string&gt; SplitTextChars(const std::string&amp; text) {
  std::vector&lt;std::string&gt; chars;
  for (size_t i = 0; i &lt; text.size();) {
    if ((text[i] &amp; 0x80) == 0) {  // ASCII
      chars.push_back(std::string(1, text[i++]));
    } else {  // UTF-8 多字节
      int bytes = 0;
      if ((text[i] &amp; 0xE0) == 0xC0)
        bytes = 2;
      else if ((text[i] &amp; 0xF0) == 0xE0)
        bytes = 3;
      else if ((text[i] &amp; 0xF8) == 0xF0)
        bytes = 4;
      else
        throw std::runtime_error("Invalid UTF-8");
      chars.push_back(text.substr(i, bytes));
      i += bytes;
    }
  }
  return chars;
}

//输入字符串，输出 input_ids 和 attention_mask
std::pair&lt;std::vector&lt;int64_t&gt;, std::vector&lt;int64_t&gt;&gt; Tokenize(
    const std::string&amp; text, const std::unordered_map&lt;std::string, int&gt;&amp; vocab,
    int maxLength = 512) {
  if (maxLength &lt; 2) {
    throw std::invalid_argument("maxLength must be at least 2");
  }

  // 预取特殊 token ID
  const int clsId = vocab.at("[CLS]");
  const int sepId = vocab.at("[SEP]");
  const int padId = vocab.at("[PAD]");
  const int unkId = vocab.at("[UNK]");

  // 初始化向量（自动 padding）
  std::vector&lt;int64_t&gt; inputIds(maxLength, padId);
  std::vector&lt;int64_t&gt; attentionMask(maxLength, 0);

  //开头加 "[CLS]"
  size_t currentIndex = 0;
  inputIds[currentIndex] = clsId;
  attentionMask[currentIndex] = 1;
  currentIndex++;

  //中间每个字作为一个 token（查 vocab 得 ID）
  auto chars = SplitTextChars(text);
  for (const auto&amp; ch : chars) {
    if (currentIndex &gt;= maxLength - 1ULL) {
      break;
    }

    const auto&amp; it = vocab.find(ch);
    inputIds[currentIndex] = (it == vocab.end() ? unkId : it-&gt;second);
    attentionMask[currentIndex] = 1;
    currentIndex++;
  }

  //结尾加 "[SEP]"
  inputIds[currentIndex] = sepId;
  attentionMask[currentIndex] = 1;

  return {inputIds, attentionMask};
}

void TestTokenize() {
  string vocabPath = "C:/Github/search/bge-small-zh-onnx/vocab.txt";
  string text = "git撤回提交";

  auto vocab = LoadVocab(vocabPath);
  const auto&amp; [inputIds, attMask] = Tokenize(text, vocab);

  // 打印前10个 token ID 验证
  cout &lt;&lt; "Input IDs (first 10): ";
  for (int i = 0; i &lt; 10; ++i) {
    cout &lt;&lt; inputIds[i] &lt;&lt; " ";
  }

  cout &lt;&lt; "\nAttention Mask (first 10): ";
  for (int i = 0; i &lt; 10; ++i) {
    cout &lt;&lt; attMask[i] &lt;&lt; " ";
  }

  cout &lt;&lt; endl;
}

int main() {
#ifdef _WIN32
  SetConsoleOutputCP(65001);
#endif

  TestTokenize();

  return 0;
}
</code></pre>
<p>无论是原始的 <code>bge-small-zh-v1.5</code> 模型，还是转换后的 ONNX 版本，其词汇表 <code>vocab.txt</code> 的内容完全一致。我们可以将输入文本切分为单个的 token 单元，并在 <code>vocab.txt</code> 中查找到对应的 ID，就能生成与我们想要嵌入的输入序列。在这个简化实现中，使用的拆分策略是<strong>逐字切分</strong>，也就是按 UTF-8 字符边界拆分字符串。</p>
<p>此外，模型使用了四个关键的特殊 token，它们在 <code>vocab.txt</code> 和 <code>special_tokens_map.json</code> 中均有定义：</p>
<ul>
<li><code>[CLS]</code>：序列起始标记，其对应位置的输出向量常用于句子级 embedding（BGE 默认使用该向量作为整个文本的表示）；</li>
<li><code>[SEP]</code>：序列结束或分隔标记，用于区分不同句子（单句输入时置于末尾）；</li>
<li><code>[PAD]</code>：填充标记，用于对齐 batch 中不同长度的序列；</li>
<li><code>[UNK]</code>：未知词标记，当输入 token 不在词汇表中时使用。</li>
</ul>
<p>最终输出的结果是：</p>
<pre><code class="language-text">Input IDs (first 10): 101 149 151 162 3059 1726 2990 769 102 0
Attention Mask (first 10): 1 1 1 1 1 1 1 1 1 0
</code></pre>
<p>我们可以打开 <code>vocab.txt</code> 进行对照，比如最后一个汉字“交”，位于文本的第770行，说明拆分的 token 是正确的（对应 ID 是769），如下图所示：</p>
<p><img alt="在词汇表中检查分词器拆分的结果" loading="lazy" data-src="https://img2024.cnblogs.com/blog/1000410/202602/1000410-20260212214009404-190299176.png" class="lazyload"></p>
<p>另一个输出结果 Attention Mask（注意力掩码） 是 Transformer 类模型（包括 BGE）中一个关键的输入张量，它的作用是告诉模型：“哪些 token 是真实内容，哪些是填充（padding）”。</p>
<h2 id="42-hugging-face-tokenizer">4.2 Hugging Face Tokenizer</h2>
<p>上述的例子只是演示分词器的基本流程，如果对照分词器的词汇表，测试文本“git撤回提交”在上述分词器的结果是：</p>
<pre><code class="language-cpp">['g', 'i', 't', '撤', '回', '提', '交']
</code></pre>
<p>但是，我们希望的分词结果是：</p>
<pre><code class="language-cpp">['git', '撤', '回', '提', '交']
</code></pre>
<p>这说明简单的 UTF-8 字符切分无法复现模型训练时的真实分词行为，会导致 embedding 质量下降，尤其在混合中英文的博客场景可能会造成不小的影响。实际上一个工业级的分词器远比“逐字切分”复杂得多，一个关键的问题就是需要在句子中预切分片段，使用<strong>WordPiece 算法</strong>贪心匹配子词。</p>
<p>面对如此复杂的分词逻辑，从零实现一个完全兼容 WordPiece 的 tokenizer 不仅耗时，还极易引入细微偏差——而这些偏差会直接导致 embedding 向量偏离预期，最终影响搜索质量。好在，我们并不需要重复造轮子。</p>
<p>Hugging Face Tokenizer 是当前 NLP 领域事实上的工业标准。它由 Hugging Face 官方维护，高度优化、跨语言支持、且与 transformers 生态无缝集成。因此，最好使用这个分词器的官方实现，确保输入序列与模型训练时完全对齐，从而释放 BGE 模型的全部语义能力。</p>
<p>不过，麻烦的是 Hugging Face Tokenizer 是使用 Rust 实现的，并且官方只提供了 Python 和 node 的绑定实现。要集成到 C++ 程序中，最好的办法就是自己封装 Hugging Face tokenizers 的 C 绑定。当然也不是封装所有的 FFI（Foreign Function Interface）接口，而是封装自己需要的接口就可以了。比如执行分词接口和计算Token的接口：</p>
<pre><code class="language-rust">use std::ffi::CStr;
use std::os::raw::c_char;
use tokenizers::{PaddingParams, Tokenizer, TruncationParams};

// === 1. 定义 C 兼容的返回结构体 ===
#[repr(C)]
pub struct TokenizerResult {
    pub input_ids: *mut i64,
    pub attention_mask: *mut i64,
    pub token_type_ids: *mut i64,
    pub length: u64,
}

// === 2. 内部状态：包装 Tokenizer ===
struct TokenizerHandle {
    tokenizer: Tokenizer,     // 用于 encode（带 padding）
    raw_tokenizer: Tokenizer, // 用于 count（无 padding）
}

// === 3. 辅助函数：将 Rust Vec 转为 C 可拥有的指针 ===
fn vec_to_c_ptr(vec: Vec&lt;i64&gt;) -&gt; *mut i64 {
    let mut boxed = vec.into_boxed_slice();
    let ptr = boxed.as_mut_ptr();
    std::mem::forget(boxed); // 防止 Rust 自动释放
    ptr
}

// === 4. 创建 tokenizer ===
#[unsafe(no_mangle)] // 禁用 name mangling，让 C 能找到符号
pub extern "C" fn tokenizer_create(tokenizer_json_path: *const c_char) -&gt; *mut std::ffi::c_void {
    if tokenizer_json_path.is_null() {
        return std::ptr::null_mut();
    }

    let path_cstr = unsafe { CStr::from_ptr(tokenizer_json_path) };
    let path_str = match path_cstr.to_str() {
        Ok(s) =&gt; s,
        Err(_) =&gt; return std::ptr::null_mut(),
    };

    let mut tokenizer = match Tokenizer::from_file(path_str) {
        Ok(t) =&gt; t,
        Err(_) =&gt; return std::ptr::null_mut(),
    };

    // 设置 padding/truncation 到 512（BGE 默认）
    tokenizer.with_padding(Some(PaddingParams {
        strategy: tokenizers::PaddingStrategy::Fixed(512),
        ..Default::default()
    }));

    if tokenizer
        .with_truncation(Some(TruncationParams {
            max_length: 512,
            ..Default::default()
        }))
        .is_err()
    {
        return std::ptr::null_mut();
    }

    let mut raw_tokenizer = tokenizer.clone();
    raw_tokenizer.with_padding(None);
    raw_tokenizer.with_truncation(None).ok();

    let handle = TokenizerHandle {
        tokenizer,
        raw_tokenizer,
    };
    Box::into_raw(Box::new(handle)) as *mut std::ffi::c_void
}

//计算句子token
#[unsafe(no_mangle)] // 禁用 name mangling，让 C 能找到符号
pub extern "C" fn tokenizer_count(handle: *mut std::ffi::c_void, text: *const c_char) -&gt; u64 {
    if handle.is_null() || text.is_null() {
        return 0;
    }

    let handle_ref = unsafe { &amp;*(handle as *mut TokenizerHandle) };

    let text_cstr = unsafe { CStr::from_ptr(text) };
    let text_str = match text_cstr.to_str() {
        Ok(s) =&gt; s,
        Err(_) =&gt; return 0,
    };

    match handle_ref.raw_tokenizer.encode(text_str, true) {
        Ok(encoding) =&gt; encoding.len() as u64,
        Err(_) =&gt; 0,
    }
}

// === 5. 销毁 tokenizer ===
#[unsafe(no_mangle)]
pub extern "C" fn tokenizer_destroy(handle: *mut std::ffi::c_void) {
    if !handle.is_null() {
        unsafe {
            let _ = Box::from_raw(handle as *mut TokenizerHandle);
            // Drop 自动调用
        }
    }
}

// === 6. 执行分词 ===
#[unsafe(no_mangle)]
pub extern "C" fn tokenizer_encode(
    handle: *mut std::ffi::c_void,
    text: *const c_char,
) -&gt; TokenizerResult {
    let default_result = TokenizerResult {
        input_ids: std::ptr::null_mut(),
        attention_mask: std::ptr::null_mut(),
        token_type_ids: std::ptr::null_mut(),
        length: 0,
    };

    if handle.is_null() || text.is_null() {
        return default_result;
    }

    let handle_ref = unsafe { &amp;*(handle as *mut TokenizerHandle) };

    let text_cstr = unsafe { CStr::from_ptr(text) };
    let text_str = match text_cstr.to_str() {
        Ok(s) =&gt; s,
        Err(_) =&gt; return default_result,
    };

    let encoding = match handle_ref.tokenizer.encode(text_str, true) {
        Ok(e) =&gt; e,
        Err(_) =&gt; return default_result,
    };

    let input_ids: Vec&lt;i64&gt; = encoding.get_ids().iter().map(|&amp;x| x as i64).collect();
    let attention_mask: Vec&lt;i64&gt; = encoding
        .get_attention_mask()
        .iter()
        .map(|&amp;x| x as i64)
        .collect();
    let token_type_ids: Vec&lt;i64&gt; = encoding.get_type_ids().iter().map(|&amp;x| x as i64).collect();
    // BGE 不需要，但 C++ 代码传了
    // let token_type_ids: Vec&lt;u32&gt; = vec![0u32; input_ids.len()];

    let len = input_ids.len(); // 应该是 512，但更通用

    TokenizerResult {
        input_ids: vec_to_c_ptr(input_ids),
        attention_mask: vec_to_c_ptr(attention_mask),
        token_type_ids: vec_to_c_ptr(token_type_ids),
        length: len as u64,
    }
}

// === 7. 释放结果内存 ===
#[unsafe(no_mangle)]
pub extern "C" fn tokenizer_result_free(result: TokenizerResult) {
    if !result.input_ids.is_null() {
        unsafe {
            let _ = Vec::from_raw_parts(
                result.input_ids,
                result.length as usize,
                result.length as usize,
            );
        }
    }

    if !result.attention_mask.is_null() {
        unsafe {
            let _ = Vec::from_raw_parts(
                result.attention_mask,
                result.length as usize,
                result.length as usize,
            );
        }
    }

    if !result.token_type_ids.is_null() {
        unsafe {
            let _ = Vec::from_raw_parts(
                result.token_type_ids,
                result.length as usize,
                result.length as usize,
            );
        }
    }
}
</code></pre>
<p>对应的 C 接口如下：</p>
<pre><code class="language-cpp">// tokenizer_result.h
#pragma once

#include &lt;stdint.h&gt;

// 定义结构体
struct TokenizerResult {
    int64_t* input_ids;
    int64_t* attention_mask;
    int64_t* token_type_ids;
    uint64_t length;
};

typedef struct TokenizerResult TokenizerResult;
</code></pre>
<pre><code class="language-cpp">// hf_tokenizer_ffi
#pragma once

#include "tokenizer_result.h"

#ifdef __cplusplus
extern "C" {
#endif

void* tokenizer_create(const char* tokenizer_json_path);
void tokenizer_destroy(void* handle);
TokenizerResult tokenizer_encode(void* handle, const char* text);
uint64_t tokenizer_count(void* handle, const char* text);
void tokenizer_result_free(TokenizerResult result);

#ifdef __cplusplus
}
#endif
</code></pre>
<p>我们在 C++ 程序中调用这个 C 绑定的接口：</p>
<pre><code class="language-cpp">#include &lt;hf_tokenizer_ffi.h&gt;

#include &lt;string&gt;
#include &lt;vector&gt;

#ifdef _WIN32
#include &lt;Windows.h&gt;
#endif

using namespace std;

void TestTokenize() {
  void* handle =
      tokenizer_create("C:/Github/search/bge-small-zh-onnx/tokenizer.json");

  if (!handle) {
    std::cerr &lt;&lt; "Failed to create tokenizer\n";
    return;
  }

  TokenizerResult result = tokenizer_encode(handle, "git撤回提交");

  if (!result.input_ids) {
    std::cerr &lt;&lt; "Tokenize failed\n";
    tokenizer_destroy(handle);
    return;
  }

  std::cout &lt;&lt; "Length: " &lt;&lt; result.length &lt;&lt; "\n";
  for (int i = 0; i &lt; 10; ++i) {
    std::cout &lt;&lt; result.input_ids[i] &lt;&lt; " ";
  }
  std::cout &lt;&lt; "\n";

  // 必须释放！
  tokenizer_result_free(result);
  tokenizer_destroy(handle);

  return;
}

int main() {
#ifdef _WIN32
  SetConsoleOutputCP(65001);
#endif

  TestTokenize();

  return 0;
}
</code></pre>
<p>运行结果如下：</p>
<pre><code>Length: 512
101 10807 3059 1726 2990 769 102 0 0 0
</code></pre>
<p>很显然，返回的前10个 ID 值中有效 ID 少了两个。再检查一下第一个 token 也就是第10808行（对应 ID 10807）的词汇：</p>
<p><img alt="在词汇表中检查分词器拆分的结果" loading="lazy" data-src="https://img2024.cnblogs.com/blog/1000410/202602/1000410-20260212214014487-390716379.png" class="lazyload"></p>
<p>正好是“git”，说明分词器正确拆分了子词。</p>
<p>验证了正确性，就要保证安全性。虽然在 C++ 程序中可以这样使用 C 接口，但是更加安全高效的使用方式是使用 RAII 原则进行封装。这一点笔者的文章<a href="https://charlee44.com/post.html?id=62bdd64be665488fa0c33abc399176e7" target="_blank" rel="noopener nofollow">《C++ 封装 C FFI 接口最佳实践：以 Hugging Face Tokenizer 为例》</a>有过相关的论述。这里直接放出封装好的结果：</p>
<pre><code class="language-cpp">// HfTokenizer.h
#pragma once

#include &lt;tokenizer_result.h&gt;

#include &lt;memory&gt;
#include &lt;string&gt;

namespace embedding {

namespace hf {
class Tokenizer {
 public:
  explicit Tokenizer(const std::string&amp; path);

  // 编译器自动生成：
  // - 析构函数（调用 Deleter）
  // - 移动构造 / 移动赋值
  // - 禁止拷贝（因为 unique_ptr 不可拷贝）

  // 其他接口方法
  uint64_t Count(const std::string&amp; text) const;

  // 向量化
  using ResultPtr =
      std::unique_ptr&lt;TokenizerResult, void (*)(TokenizerResult*)&gt;;
  ResultPtr Encode(const std::string&amp; text) const;

 private:
  std::unique_ptr&lt;void, void (*)(void*)&gt; handle;
};

}  // namespace hf

}  // namespace embedding
</code></pre>
<pre><code class="language-cpp">// HfTokenizer.cpp
#include "HfTokenizer.h"

#include &lt;hf_tokenizer_ffi.h&gt;

#include &lt;stdexcept&gt;

#ifdef __cplusplus
static_assert(std::is_standard_layout_v&lt;TokenizerResult&gt; &amp;&amp;
                  std::is_trivially_copyable_v&lt;TokenizerResult&gt;,
              "TokenizerResult must be C ABI compatible");
#endif

namespace embedding {

namespace hf {

static void HandleDeleter(void* handle) noexcept {
  if (handle) {
    tokenizer_destroy(handle);
  }
}

static void ResultDeleter(TokenizerResult* p) noexcept {
  if (p) {
    tokenizer_result_free(*p);
    delete p;
  }
}

Tokenizer::Tokenizer(const std::string&amp; path)
    : handle(tokenizer_create(path.c_str()), HandleDeleter) {
  if (!handle) {
    throw std::runtime_error("Failed to create tokenizer from " + path);
  }
}

uint64_t Tokenizer::Count(const std::string&amp; text) const {
  return tokenizer_count(handle.get(), text.c_str());
}

Tokenizer::ResultPtr Tokenizer::Encode(const std::string&amp; text) const {
  auto result = std::make_unique&lt;TokenizerResult&gt;(
      tokenizer_encode(handle.get(), text.c_str()));
  return {result.release(), ResultDeleter};
  // TokenizerResult raw = tokenizer_encode(handle.get(), text.c_str());
  // return {new TokenizerResult(std::move(raw)), ResultDeleter};
};

}  // namespace hf

}  // namespace embedding
</code></pre>
<h1 id="5-嵌入器">5. 嵌入器</h1>
<p>在解决了分词器的问题之后，就可以开始实现句子嵌入的功能了。如前文提到的，该功能可以引入 ONNX Runtime 组件来实现。为了方便进一步使用，我们将这个功能封装为嵌入器：</p>
<pre><code class="language-cpp">// BgeOnnxEmbedder.h
#pragma once

#include &lt;memory&gt;
#include &lt;string&gt;
#include &lt;vector&gt;

namespace embedding {

namespace hf {
class Tokenizer;
}

class BgeOnnxEmbedder {
 public:
  explicit BgeOnnxEmbedder(const std::string&amp; modelPath,
                           const hf::Tokenizer&amp; tokenizer);
  ~BgeOnnxEmbedder();

  const int64_t&amp; EmbeddingDim() const;

  std::vector&lt;float&gt; Embed(const std::string&amp; text) const;

 private:
  class Impl;  // 前向声明
  std::unique_ptr&lt;Impl&gt; impl;
};

}  // namespace embedding
</code></pre>
<pre><code class="language-cpp">// BgeOnnxEmbedder.cpp
#include "BgeOnnxEmbedder.h"

#include &lt;onnxruntime_cxx_api.h&gt;

#include "HfTokenizer.h"
#include "Util/StringEncode.h"

namespace embedding {

class BgeOnnxEmbedder::Impl {
 public:
  Ort::Env&amp; GetOrtEnv() {
    static Ort::Env env(ORT_LOGGING_LEVEL_WARNING, "BgeOnnxEmbedder");
    return env;
  }

  const int64_t&amp; EmbeddingDim() const { return embeddingDim; }

  explicit Impl(const std::string&amp; modelPath, const hf::Tokenizer&amp; tokenizer)
      : session{GetOrtEnv(),
#ifdef _WIN32
                util::StringEncode::Utf8StringToWideString(modelPath).c_str(),
#else
                modelPath.c_str(),
#endif
                Ort::SessionOptions()},
        memInfo{Ort::MemoryInfo::CreateCpu(OrtDeviceAllocator, OrtMemTypeCPU)},
        tokenizer(tokenizer),
        embeddingDim(0) {

    //
    const auto&amp; outputInfo = session.GetOutputTypeInfo(0);
    const auto&amp; tensorInfo = outputInfo.GetTensorTypeAndShapeInfo();
    const auto&amp; shape = tensorInfo.GetShape();

    // 假设输出是 [batch, seq, dim] 或 [batch, dim]
    // 我们取最后一个非 -1 的维度
    for (auto it = shape.rbegin(); it != shape.rend(); ++it) {
      if (*it != -1) {
        embeddingDim = *it;
        break;
      }
    }

    if (embeddingDim == 0) {
      throw std::runtime_error(
          "Failed to infer embedding dimension from ONNX model.");
    }
  }

  std::vector&lt;float&gt; Embed(const std::string&amp; text) const {
    hf::Tokenizer::ResultPtr result = tokenizer.Encode(text);
    if (!result) {
      throw std::runtime_error("tokenizer_encode failed");
    }

    // 定义张量维度
    int64_t seqLen = static_cast&lt;int64_t&gt;(result-&gt;length);
    std::vector&lt;int64_t&gt; inputShape = {1, seqLen};
    size_t dataByteCount = sizeof(int64_t) * seqLen;

    Ort::Value inputIdsTensor = Ort::Value::CreateTensor(
        memInfo.GetConst(), result-&gt;input_ids, dataByteCount, inputShape.data(),
        inputShape.size(),
        ONNXTensorElementDataType::ONNX_TENSOR_ELEMENT_DATA_TYPE_INT64);

    Ort::Value attentionMaskTensor = Ort::Value::CreateTensor(
        memInfo.GetConst(), result-&gt;attention_mask, dataByteCount,
        inputShape.data(), inputShape.size(),
        ONNXTensorElementDataType::ONNX_TENSOR_ELEMENT_DATA_TYPE_INT64);

    Ort::Value tokenTypeIdsTensor = Ort::Value::CreateTensor(
        memInfo.GetConst(), result-&gt;token_type_ids, dataByteCount,
        inputShape.data(), inputShape.size(),
        ONNXTensorElementDataType::ONNX_TENSOR_ELEMENT_DATA_TYPE_INT64);

    // 输入名必须与模型定义一致
    const char* inputNames[] = {"input_ids", "attention_mask",
                                "token_type_ids"};
    const char* outputNames[] = {"last_hidden_state"};

    // 把三个输入张量放进数组
    std::vector&lt;Ort::Value&gt; inputs;
    inputs.push_back(std::move(inputIdsTensor));
    inputs.push_back(std::move(attentionMaskTensor));
    inputs.push_back(std::move(tokenTypeIdsTensor));

    // 执行推理
    auto outputs = session.Run(Ort::RunOptions(),  // 运行选项（通常 nullptr）
                               inputNames,         // 输入名数组
                               inputs.data(),  // 输入张量数组
                               inputs.size(),  // 输入数量（3）
                               outputNames,    // 输出名数组
                               1               // 输出数量（1）
    );

    // 获取输出信息
    auto&amp; output_tensor = outputs[0];
    auto output_shape = output_tensor.GetTensorTypeAndShapeInfo().GetShape();
    if (output_shape.size() != 3 || output_shape[0] != 1) {
      throw std::runtime_error("Unexpected output shape");
    }

    // 获取输出张量的原始 float 指针
    const float* outputData = outputs[0].GetTensorData&lt;float&gt;();

    // 提取 [CLS] token 的 embedding（第0个token）
    int64_t hiddenSize = output_shape[2];
    std::vector&lt;float&gt; embedding(outputData, outputData + hiddenSize);

    // L2 归一化（BGE 要求）
    float norm = 0.0f;
    for (float v : embedding) norm += v * v;
    norm = std::sqrt(norm);
    if (norm &gt; 1e-8) {
      for (float&amp; v : embedding) v /= norm;
    }

    return embedding;
  }

 private:
  mutable Ort::Session session;
  Ort::MemoryInfo memInfo;
  const hf::Tokenizer&amp; tokenizer;
  int64_t embeddingDim;
};

BgeOnnxEmbedder::BgeOnnxEmbedder(const std::string&amp; modelPath,
                                 const hf::Tokenizer&amp; tokenizer)
    : impl(std::make_unique&lt;Impl&gt;(modelPath, tokenizer)) {}

BgeOnnxEmbedder::~BgeOnnxEmbedder() = default;  // 此时 Impl 已定义，可安全析构

const int64_t&amp; BgeOnnxEmbedder::EmbeddingDim() const {
  return impl-&gt;EmbeddingDim();
}

std::vector&lt;float&gt; BgeOnnxEmbedder::Embed(const std::string&amp; text) const {
  return impl-&gt;Embed(text);
}

}  // namespace embedding
</code></pre>
<p>在调用 ONNX Runtime 执行推理之前，我们必须将分词器输出的原始 token 序列转换为模型能够理解的张量（Tensor）格式。这正是 <code>inputIdsTensor</code>、<code>attentionMaskTensor</code> 和 <code>tokenTypeIdsTensor</code> 三个张量的由来——它们共同构成了 Transformer 模型的标准输入三元组。</p>
<ul>
<li><strong><code>input_ids</code></strong>（对应 <code>inputIdsTensor</code>）：<br>
这是最核心的输入，表示文本经过分词后得到的 token ID 序列。每个 ID 是词汇表（<code>vocab.txt</code>）中的索引，模型通过嵌入层（Embedding Layer）将其映射为稠密向量。例如，句子 “git撤回提交” 经 Hugging Face 分词器处理后，会变成 <code>[101, 10807, 3059, 1726, 2990, 769, 102, 0, 0, ...]</code>，其中 <code>101</code> 是 <code>[CLS]</code>，<code>102</code> 是 <code>[SEP]</code>，其余为实际内容或填充（<code>[PAD]</code>，ID=0）。这个序列被组织成形状为 <code>(1, seq_len)</code> 的二维张量（batch_size=1），作为模型的主输入。</li>
<li><strong><code>attention_mask</code></strong>（对应 <code>attentionMaskTensor</code>）<br>
由于 BGE 模型要求所有输入序列长度对齐（此处固定为 512），短文本会被 <code>[PAD]</code> 填充。但模型不应“关注”这些无意义的填充位置。<code>attention_mask</code> 就是一个二值掩码：真实 token 对应 <code>1</code>，填充部分对应 <code>0</code>。Transformer 的自注意力机制会利用该掩码，在计算注意力权重时自动忽略 <code>[PAD]</code>，从而避免噪声干扰语义表示。</li>
<li><strong><code>token_type_ids</code></strong>（对应 <code>tokenTypeIdsTensor</code>）<br>
该输入主要用于区分句子对任务（如问答、句子相似度判断），例如 <code>[A句][SEP][B句]</code> 中 A 和 B 分属不同 segment。但在单句嵌入场景（如我们的博客搜索）中，整个输入只包含一个句子，因此所有有效 token 的 <code>token_type_id</code> 均为 <code>0</code>。尽管 BGE 模型在训练时可能使用了该字段，但在推理阶段即使传入全零向量，也不会影响 <code>[CLS]</code> 表示的质量。出于接口兼容性考虑，我们仍按规范提供此张量。</li>
</ul>
<p>这三个张量通过 ONNX Runtime 的 C++ API 被封装为 <code>Ort::Value</code> 对象，并以名称匹配的方式传入模型。ONNX 模型内部会根据 <code>config.json</code> 中定义的输入节点名（通常为 <code>"input_ids"</code>、<code>"attention_mask"</code>、<code>"token_type_ids"</code>）正确绑定数据。随后，模型执行前向传播，输出最后一层隐藏状态（<code>last_hidden_state</code>），其形状为 <code>(1, seq_len, hidden_size)</code>。我们从中提取第 0 个位置（即 <code>[CLS]</code> token）的向量作为整句的语义表示，并按 BGE 官方推荐进行 <strong>L2 归一化</strong>——这一步至关重要，因为后续的向量相似度检索（如余弦相似度）依赖于单位向量空间中的点积等价性。</p>
<blockquote>
<p>嵌入模型 bge-small-zh-v1.5 最大输入长度 = 512 tokens，嵌入向量维度 = 512维。参数值都是 512 ，但是两者并没有绝对的关系，请注意区分。</p>
</blockquote>
<p>至此，一段任意长度的中文文本就被成功转化为一个 512 维（<code>bge-small-zh-v1.5</code> 的输出维度）的标准化语义向量。眼见才为实，很多人（包括笔者）想看看某个文本（比如前面的“git撤回提交”）的向量化输出，那么可以先测试一下：</p>
<pre><code class="language-cpp">#include &lt;string&gt;
#include &lt;vector&gt;

#include "BgeOnnxEmbedder.h"
#include "HfTokenizer.h"

#ifdef _WIN32
#include &lt;Windows.h&gt;
#endif

using namespace std;
using namespace embedding;

int main() {
#ifdef _WIN32
  SetConsoleOutputCP(65001);
#endif

  hf::Tokenizer tokenizer("C:/Github/search/bge-small-zh-onnx/tokenizer.json");
  BgeOnnxEmbedder bgeOnnxEmbedder(
      "C:/Github/search/bge-small-zh-onnx/model.onnx", tokenizer);

  string text = "git撤回提交";
  std::vector&lt;float&gt; embedding = bgeOnnxEmbedder.Embed(text);

  for (auto value : embedding) {
    printf("%lf\t", value);
  }

  return 0;
}
</code></pre>
<p>输出结果是：</p>
<pre><code class="language-text">-0.031685       -0.035368       0.069596        -0.030640       -0.011818       -0.023779       -0.043855       0.0064050.067668        0.004901        0.025522        -0.269526       -0.024420       0.065645    ...      0.003664        -0.002409      -0.061532        0.002052        -0.056993       -0.057258       -0.073969       -0.020673       0.044549        0.005769-0.009345       -0.004969       0.013322        0.041202
</code></pre>
<p>其实也没什么特别的，就是一个高维向量；这个向量是也机器理解的，所以也没什么可阅读性。</p>
<p>另外补充一点，这个嵌入器实现是基于 PIMPL 技术实现的，笔者的文章<a href="https://charlee44.com/post.html?id=b89125a9380d44bc865557f3d1781c9f" target="_blank" rel="noopener nofollow">《为什么现代 C++ 库都用 PIMPL？一场关于封装、依赖与安全的演进》</a>就以这个嵌入器为例论述了 PIMPL 技术。</p>
<h1 id="6-语义分段器">6. 语义分段器</h1>
<p>在成功实现句子嵌入功能后，似乎语义化搜索的核心难题已经解决。然而，真正的挑战才刚刚开始：<strong>嵌入模型对输入长度是有限制的</strong>。以我们使用的 <code>bge-small-zh-v1.5</code> 为例，其最大支持的 token 长度为 512。这意味着，若直接将一篇长博客文章（可能包含数千字）送入模型，超出部分会被截断，导致大量语义信息丢失。</p>
<p>乍看之下，解决方案似乎很简单：按 512 token 的长度对文档进行机械切分即可。但这种“暴力分块”会带来严重问题：</p>
<ol>
<li><strong>破坏语义完整性</strong>：一个完整的句子或段落可能被从中切断，导致生成的向量无法准确表达原始含义；</li>
<li><strong>割裂上下文关联</strong>：相邻但被强行分割的块之间缺乏语义衔接，影响后续检索的相关性；</li>
<li><strong>浪费嵌入容量</strong>：短段落单独成块会造成大量填充（padding），降低向量密度和检索效率。</li>
</ol>
<p>因此，<strong>关键不在于“如何切”，而在于“在哪里切”</strong>——我们需要的是<strong>具有语义边界的分块</strong>，而非任意位置的截断。</p>
<p>对于 Markdown 格式的博客文章而言，这一目标天然具备实现基础：<strong>标题（如 <code>#</code>、<code>##</code> 等）本身就是清晰的语义边界</strong>。每个标题下的内容通常构成一个逻辑自洽的主题单元（例如“安装步骤”、“原理分析”、“常见问题”）。基于此，我们可以设计一个 <strong>Markdown 语义分段器（<code>MdSemanticSplitter</code>）</strong>，其核心任务不是生成最终用于嵌入的“块”（chunk），而是先将整篇文档<strong>按语义结构拆分为若干“语义段”（semantic sections）</strong>。</p>
<blockquote>
<p>📌 注意：<strong>语义分段 ≠ 最终分块</strong>。<br>
分段是预处理阶段，关注的是<strong>内容结构与逻辑边界</strong>；而分块（chunking）是后续步骤，关注的是<strong>如何将语义段适配到模型长度限制内</strong>（例如合并短段、滑动窗口重叠等）。</p>
</blockquote>
<p>这种两阶段策略具有良好的扩展性：无论是 Markdown、Word 还是 HTML 文档，只要能提取出语义层级结构（如章节、段落、列表、代码块等），就可以先通过对应的 <strong>语义分段器</strong> 得到结构化片段，再交由通用的 <strong>文本分块器（<code>TextChunker</code>）</strong> 进行长度适配与向量化准备。</p>
<p>因此，在我们的系统中，语义分段器<code>MdSemanticSplitter</code> 的职责非常明确：<strong>解析 Markdown，识别标题层级，按语义边界切分出完整的段落单元</strong>，为后续高效、准确的嵌入打下坚实基础。具体的实现如下：</p>
<pre><code class="language-cpp">// SemanticBlock.h
#pragma once

#include &lt;string&gt;
#include &lt;vector&gt;

// 语义块
struct SemanticBlock {
  std::string context;                //上下文，通常是标签
  std::vector&lt;std::string&gt; contents;  //文本内容
};
</code></pre>
<pre><code class="language-cpp">// MdSemanticSplitter.h
#pragma once

#include "SemanticBlock.h"

namespace embedding {

class MdSemanticSplitter {
 public:
  std::vector&lt;SemanticBlock&gt; Split(const std::string&amp; title,
                                   const std::string&amp; content,
                                   const std::string&amp; summary) const;
};
}  // namespace embedding
</code></pre>
<pre><code class="language-cpp">// MdSemanticSplitter.cpp
#include "MdSemanticSplitter.h"

#include &lt;md4c.h&gt;

#include &lt;iostream&gt;
#include &lt;list&gt;

using namespace std;

namespace embedding {

enum class BlockKind { Heading, Paragraph, ListItem, BlockQuote };

struct Block {
  BlockKind kind;
  int level = 0;  // heading level
  std::string text;
};

struct ParseContext {
  std::vector&lt;Block&gt; result;
  std::vector&lt;Block&gt; blockStack;  // Block栈
  // int activeDepth = 0;            //深度计数
};

static int EnterBlock(MD_BLOCKTYPE type, void* detail, void* userdata) {
  auto* ctx = static_cast&lt;ParseContext*&gt;(userdata);

  Block block{};
  switch (type) {
    case MD_BLOCK_H: {
      auto* h = static_cast&lt;MD_BLOCK_H_DETAIL*&gt;(detail);
      block.kind = BlockKind::Heading;
      block.level = h-&gt;level;
      break;
    }
    case MD_BLOCK_P: {
      block.kind = BlockKind::Paragraph;
      break;
    }
    case MD_BLOCK_LI: {
      block.kind = BlockKind::ListItem;
      break;
    }
    case MD_BLOCK_QUOTE: {
      block.kind = BlockKind::BlockQuote;
      break;
    }
    default:
      // 其他 block（如 UL / OL / CODE）不处理
      break;
  }

  ctx-&gt;blockStack.emplace_back(std::move(block));
  return 0;
}

static int TextCallback(MD_TEXTTYPE type, const MD_CHAR* text, MD_SIZE size,
                        void* userdata) {
  auto* ctx = static_cast&lt;ParseContext*&gt;(userdata);
  if (ctx-&gt;blockStack.empty()) return 0;

  // 只接收真实可读文本
  if (type == MD_TEXT_NORMAL || type == MD_TEXT_CODE) {
    ctx-&gt;blockStack.back().text.append(text, size);
  }

  return 0;
}

static int LeaveBlock(MD_BLOCKTYPE type, void*, void* userdata) {
  auto* ctx = static_cast&lt;ParseContext*&gt;(userdata);
  if (ctx-&gt;blockStack.empty()) {
    return 0;
  }

  Block block = std::move(ctx-&gt;blockStack.back());
  ctx-&gt;blockStack.pop_back();

  bool emit = false;
  switch (type) {
    case MD_BLOCK_H:
    case MD_BLOCK_P:
    case MD_BLOCK_LI:
    case MD_BLOCK_QUOTE:
      emit = true;
      break;
    default:
      break;
  }

  if (emit &amp;&amp; !block.text.empty()) {
    ctx-&gt;result.emplace_back(std::move(block));
  }

  return 0;
}

static int EnterSpan(MD_SPANTYPE /*type*/, void* /*detail*/,
                     void* /*userdata*/) {
  return 0;
}

static int LeaveSpan(MD_SPANTYPE /*type*/, void* /*detail*/,
                     void* /*userdata*/) {
  return 0;
}

std::vector&lt;SemanticBlock&gt; MdSemanticSplitter::Split(
    const std::string&amp; title, const std::string&amp; content,
    const std::string&amp; summary) const {
  ParseContext ctx{};

  MD_PARSER parser{};
  parser.abi_version = 0;
  parser.enter_block = EnterBlock;
  parser.leave_block = LeaveBlock;
  parser.enter_span = EnterSpan;
  parser.leave_span = LeaveSpan;
  parser.text = TextCallback;
  parser.flags = MD_FLAG_COLLAPSEWHITESPACE;  //合并空格

  md_parse(content.data(), (MD_SIZE)content.size(), &amp;parser, &amp;ctx);

  // std::cout &lt;&lt; "[Parsed blocks] " &lt;&lt; ctx.result.size() &lt;&lt; "\n\n";
  // for (const auto&amp; b : ctx.result) {
  //  switch (b.kind) {
  //    case BlockKind::Heading:
  //      std::cout &lt;&lt; "[H" &lt;&lt; b.level &lt;&lt; "] ";
  //      break;
  //    case BlockKind::Paragraph:
  //      std::cout &lt;&lt; "[P] ";
  //      break;
  //    case BlockKind::ListItem:
  //      std::cout &lt;&lt; "[LI] ";
  //      break;
  //    case BlockKind::BlockQuote:
  //      std::cout &lt;&lt; "[Q] ";
  //      break;
  //  }
  //  std::cout &lt;&lt; b.text &lt;&lt; "\n";
  //}

  vector&lt;SemanticBlock&gt; semanticBlocks;
  {
    SemanticBlock semanticBlock;
    semanticBlock.context = title;
    semanticBlock.contents = {summary};
    semanticBlocks.emplace_back(std::move(semanticBlock));
  }

  std::list&lt;std::pair&lt;int, std::string&gt;&gt; currentTitleChain;  // 存 (级别, 文本)
  std::vector&lt;std::string&gt; currentBlockContents;

  for (auto&amp; b : ctx.result) {
    if (b.kind == BlockKind::Heading) {
      //遇到新标题，存入数据
      if (!currentBlockContents.empty()) {
        string context;
        for (auto it = currentTitleChain.begin(); it != currentTitleChain.end();
             ++it) {
          context += it-&gt;second;  // 只取标题文本
          if (std::next(it) != currentTitleChain.end()) context += "｜";
        }
        SemanticBlock semanticBlock;
        semanticBlock.context = std::move(context);
        semanticBlock.contents = std::move(currentBlockContents);
        semanticBlocks.emplace_back(std::move(semanticBlock));
        currentBlockContents = {};
      }

      // 弹出所有级别 &gt;= 当前标题级别的祖先（精准回溯）
      while (!currentTitleChain.empty() &amp;&amp;
             currentTitleChain.back().first &gt;= b.level) {
        currentTitleChain.pop_back();
      }
      currentTitleChain.emplace_back(b.level,
                                     std::move(b.text));  // 存级别+文本
    } else {
      currentBlockContents.emplace_back(std::move(b.text));
    }
  }
  if (!currentBlockContents.empty()) {
    string context;
    for (auto it = currentTitleChain.begin(); it != currentTitleChain.end();
         ++it) {
      context += it-&gt;second;  // 只取标题文本
      if (std::next(it) != currentTitleChain.end()) context += "｜";
    }

    SemanticBlock semanticBlock;
    semanticBlock.context = std::move(context);
    semanticBlock.contents = std::move(currentBlockContents);
    semanticBlocks.emplace_back(std::move(semanticBlock));
    currentBlockContents = {};
  }

  return semanticBlocks;
}

}  // namespace embedding
</code></pre>
<p>这段 <code>MdSemanticSplitter</code> 的实现基于 <strong>MD4C</strong> —— 一个轻量、符合 CommonMark 标准的 C 语言 Markdown 解析器。它不依赖正则表达式或字符串暴力匹配，而是通过事件驱动的方式，在解析过程中逐块（block）识别 Markdown 结构。</p>
<p>具体来说，我们注册了三个核心回调函数：</p>
<ul>
<li><code>EnterBlock</code>：当进入一个新块（如标题、段落、列表项等）时触发，记录其类型和层级；</li>
<li><code>TextCallback</code>：收集块内的原始文本内容（包括普通文本和行内代码）；</li>
<li><code>LeaveBlock</code>：当离开一个块时，将已收集的文本封装为结构化 <code>Block</code> 并暂存。</li>
</ul>
<p>通过维护一个 <strong>标题栈（<code>currentTitleChain</code>）</strong>，我们能动态追踪当前内容所处的章节路径。每当遇到新的标题（<code>#</code>, <code>##</code> 等），就将此前累积的所有段落、列表项等内容打包为一个 <code>SemanticBlock</code>，其上下文（<code>context</code>）由从根到当前标题的完整路径构成（例如 <code>"部署｜Docker 配置｜环境变量"</code>）。这样，每个语义块不仅包含原始文本，还携带了明确的结构化上下文信息，极大提升了后续嵌入的语义准确性。</p>
<blockquote>
<p>补充一个细节，对于每一篇博文，第一个语义分段的上下文是文章标题，内容是文章摘要。这样做可以保证文档的语义锚点，确保即使后续内容被拆分，检索系统仍能准确理解该文档的核心主题。</p>
</blockquote>
<p>最终输出的 <code>std::vector&lt;SemanticBlock&gt;</code> 即为文档的“语义骨架”——它保留了 Markdown 的逻辑层次，同时为下一步的 <strong>文本分块（chunking）</strong> 提供了高质量、结构感知的输入单元。</p>
<h1 id="7-文本分块器">7. 文本分块器</h1>
<p>在语义分段器将 Markdown 文档解析为结构化的 <strong>语义段</strong>（<code>SemanticBlock</code>）之后，我们距离真正可嵌入的输入单元仍差一步：<strong>将这些语义段适配到嵌入模型的长度限制内</strong>。</p>
<p>嵌入模型（如 <code>bge-small-zh-v1.5</code>）对输入长度有硬性上限（通常为 512 tokens）。然而，现实中的博客内容千差万别——有的章节寥寥数语，有的段落洋洋洒洒。若直接对每个语义段独立嵌入，会面临两个极端问题：</p>
<ol>
<li><strong>过长段落被截断</strong>：超出 512 tokens 的内容会被无情丢弃，导致关键信息丢失；</li>
<li><strong>过短段落浪费容量</strong>：一个仅含两句话的段落占据整个 512-token 输入槽位，大量位置被 <code>[PAD]</code> 填充，不仅降低向量密度，还可能稀释语义信号。</li>
</ol>
<p>因此，我们需要一个智能的 <strong>文本分块器</strong>（<code>TextChunker</code>），它不仅要处理长度约束，更要<strong>在保留语义连贯性的前提下，动态地拆分或合并内容</strong>。具体来说，它的核心职责包括：</p>
<ul>
<li><strong>按 token 数精确切分长段</strong>：当单个语义段超过最大长度时，以句子为单位进行滑动窗口切分，避免在词或字中间暴力截断；</li>
<li><strong>合并相邻短段</strong>：将多个语义相关但各自过短的段落组合成一个紧凑块，提升嵌入效率；</li>
<li><strong>注入上下文信息</strong>：在分块时保留其所属的标题路径（如 <code>"数据库｜连接池配置｜HikariCP 参数详解"</code>），作为前缀拼接到文本开头，使嵌入向量“知道”自己来自哪一部分内容。</li>
</ul>
<p>这种策略确保了每个最终用于嵌入的 <strong>文本块</strong>（chunk）既满足模型输入约束，又尽可能完整、自洽地表达一个局部语义单元。</p>
<blockquote>
<p>💡 这正是 <strong>RAG</strong>（Retrieval-Augmented Generation）系统中“检索”环节的关键前置步骤。<br>
在典型的 RAG 架构中，外部知识库首先被切分为高质量 chunks，再经嵌入模型转化为向量并存入索引。当用户提问时，系统通过语义相似度召回最相关的若干 chunks，将其作为上下文提供给大语言模型生成答案。<br>
因此，<strong>分块质量直接决定了检索精度，进而影响最终回答的准确性与相关性</strong>。</p>
</blockquote>
<p>在我们的轻量级博客搜索系统中，虽然没有后续的大模型生成环节，但“检索即服务”的思想依然适用——用户输入的查询需要精准匹配到最相关的文章片段。而这一切，都始于一个精心设计的文本分块器。</p>
<p>接下来，我们将基于 Hugging Face Tokenizer 提供的 token 计数能力，实现一个兼顾效率与语义的 <code>TextChunker</code>，为构建完整的向量索引铺平道路：</p>
<pre><code class="language-cpp">// TextChunker.h
#pragma once

#include &lt;vector&gt;

#include "HfTokenizer.h"
#include "SemanticBlock.h"

namespace embedding {

class TextChunker {
 public:
  explicit TextChunker(size_t maxTokens, const hf::Tokenizer&amp; tokenizer);

  // 支持多个语义块
  std::vector&lt;std::string&gt; SemanticBlocks(
      std::vector&lt;SemanticBlock&gt; semanticBlockList) const;

  // 支持多个块元素
  std::vector&lt;std::string&gt; Blocks(std::vector&lt;std::string&gt; blockList) const;

  // 支持段落或长文本
  std::vector&lt;std::string&gt; Paragraph(std::string text) const;

 private:
  // 按中文标点切分为句子（保留标点）
  std::vector&lt;std::string&gt; SplitIntoSentences(
      const std::string&amp; paragraph) const;

  // 将句子列表合并为满足 token 限制的 chunks
  std::vector&lt;std::string&gt; MergeSentencesToChunks(
      std::vector&lt;std::string&gt;&amp;&amp; sentences) const;

  size_t maxTokens;
  const hf::Tokenizer&amp; tokenizer;
};

}  // namespace embedding
</code></pre>
<pre><code class="language-cpp">// TextChunker.cpp
#include "TextChunker.h"

#include &lt;iostream&gt;
#include &lt;unordered_set&gt;

using namespace std;

namespace embedding {

TextChunker::TextChunker(size_t maxTokens, const hf::Tokenizer&amp; tokenizer)
    : maxTokens(maxTokens), tokenizer(tokenizer) {}

std::vector&lt;std::string&gt; TextChunker::SemanticBlocks(
    std::vector&lt;SemanticBlock&gt; semanticBlockList) const {
  vector&lt;std::string&gt; totalChunkList;

  for (auto&amp; semanticBlock : semanticBlockList) {
    string contextText = semanticBlock.context + "\n";
    vector&lt;std::string&gt; chunkList = Blocks(std::move(semanticBlock.contents));

    // Token计数不用太严格，maxTokens肯定小于nlp模型处理数，且上下文非常小
    for (auto&amp; chunk : chunkList) {
      chunk = contextText + std::move(chunk);
    }

    totalChunkList.insert(totalChunkList.end(),
                          std::make_move_iterator(chunkList.begin()),
                          std::make_move_iterator(chunkList.end()));
  }

  return totalChunkList;
}

std::vector&lt;std::string&gt; TextChunker::Blocks(
    std::vector&lt;std::string&gt; blockList) const {
  // 按照文档中的块元素进行合并
  vector&lt;std::string&gt; chunkList;
  vector&lt;size_t&gt; chunkTokenCount;
  string currentChunk;  //当前块
  size_t totalTokenCount = 0;
  for (size_t i = 0; i &lt; blockList.size(); ++i) {
    string currentText(std::move(blockList[i]));
    currentText += '\n';
    size_t currentTokenCount = tokenizer.Count(currentText.c_str());
    if (totalTokenCount + currentTokenCount &gt; maxTokens) {
      if (!currentChunk.empty()) {
        chunkList.emplace_back(std::move(currentChunk));
        chunkTokenCount.emplace_back(totalTokenCount);
      }
      currentChunk = std::move(currentText);
      totalTokenCount = currentTokenCount;
    } else {
      currentChunk += std::move(currentText);
      totalTokenCount += currentTokenCount;
    }
  }
  if (!currentChunk.empty()) {
    chunkList.emplace_back(std::move(currentChunk));
    chunkTokenCount.emplace_back(totalTokenCount);
  }

  // 有的块元素本身就超过maxTokens，继续拆分
  vector&lt;std::string&gt; resultChunkList;
  for (size_t ci = 0; ci &lt; chunkList.size(); ++ci) {
    if (chunkTokenCount[ci] &gt; maxTokens) {
      vector&lt;std::string&gt; subChunks = Paragraph(std::move(chunkList[ci]));
      for (size_t si = 0; si &lt; subChunks.size(); ++si) {
        string chunk = std::move(subChunks[si]);
        if (si + 1 != subChunks.size()) {
          chunk += '\n';
        }
        resultChunkList.emplace_back(chunk);
      }
    } else {
      resultChunkList.emplace_back(std::move(chunkList[ci]));
    }
  }
  return resultChunkList;
}

// Todo:对于RAG要处理
//「句子单句 &gt; maxTokens」的 fallback
//增加 token-based overlap
std::vector&lt;std::string&gt; TextChunker::Paragraph(std::string text) const {
  return MergeSentencesToChunks(SplitIntoSentences(text));
}

// 按中文标点切分为句子（保留标点）
std::vector&lt;std::string&gt; TextChunker::SplitIntoSentences(
    const std::string&amp; paragraph) const {
  if (paragraph.empty()) {
    return {};
  }

  // 定义中文句末标点集合（UTF-8 字符串）
  static const std::unordered_set&lt;std::string&gt; delimiters = {"。", "！", "？",
                                                             "；", "……"};

  std::vector&lt;std::string&gt; chunks;
  size_t i = 0;
  size_t start = 0;
  const char* data = paragraph.data();
  size_t len = paragraph.size();

  while (i &lt; len) {
    // 判断当前位置是否是某个分隔符的起始
    bool found = false;
    for (const auto&amp; delim : delimiters) {
      if (paragraph.compare(i, delim.size(), delim) == 0) {
        // 找到分隔符：提取 [start, i + delim.size())
        std::string chunk = paragraph.substr(start, i - start + delim.size());
        if (!chunk.empty()) {
          chunks.push_back(chunk);
        }
        // 跳过分隔符
        i += delim.size();
        start = i;
        found = true;
        break;
      }
    }

    if (!found) {
      // 普通字符：向前移动一个 UTF-8 字符
      unsigned char c = static_cast&lt;unsigned char&gt;(data[i]);
      if ((c &amp; 0x80) == 0) {
        i += 1;
      } else if ((c &amp; 0xE0) == 0xC0) {
        i += 2;
      } else if ((c &amp; 0xF0) == 0xE0) {
        i += 3;
      } else if ((c &amp; 0xF8) == 0xF0) {
        i += 4;
      } else {
        i += 1;  // invalid UTF-8 fallback
      }
    }
  }

  // 处理末尾没有标点的剩余部分
  if (start &lt; len) {
    std::string last = paragraph.substr(start);
    if (!last.empty()) {
      chunks.push_back(last);
    }
  }

  return chunks;
}

std::vector&lt;std::string&gt; TextChunker::MergeSentencesToChunks(
    std::vector&lt;std::string&gt;&amp;&amp; sentences) const {
  vector&lt;string&gt; chunkList;
  string currentChunk;  //当前块
  size_t totalTokenCount = 0;
  for (size_t i = 0; i &lt; sentences.size(); ++i) {
    // cout &lt;&lt; mdBlockList[i].level &lt;&lt; '\t' &lt;&lt; mdBlockList[i].text &lt;&lt; '\n';
    string currentText(std::move(sentences[i]));
    size_t currentTokenCount = tokenizer.Count(currentText.c_str());
    if (totalTokenCount + currentTokenCount &gt; maxTokens) {
      chunkList.emplace_back(std::move(currentChunk));
      currentChunk = std::move(currentText);
      totalTokenCount = currentTokenCount;
    } else {
      currentChunk += std::move(currentText);
      totalTokenCount += currentTokenCount;
    }
  }
  if (!currentChunk.empty()) {
    chunkList.emplace_back(std::move(currentChunk));
  }

  return chunkList;
}

}  // namespace embedding
</code></pre>
<p>上述 <code>TextChunker</code> 的设计遵循一个朴素但有效的原则：<strong>优先尊重文档原有的结构单元，再在必要时进行语义感知的切分</strong>。</p>
<p>它的入口方法 <code>SemanticBlocks()</code> 接收由 <code>MdSemanticSplitter</code> 产出的语义段列表。每个语义段都携带了完整的上下文路径（如 <code>"部署｜容器化｜Dockerfile 编写"</code>），我们将其作为前缀附加到该段所有内容块的开头——这样，即使某段内容被单独检索到，其向量也能“记住”自己所属的主题层级，极大增强搜索的相关性。</p>
<p>内部处理分为两个层次：</p>
<ol>
<li><strong>块级合并（<code>Blocks</code>）</strong><br>
首先尝试将同一语义段内的多个小块（如若干段落、列表项）按顺序拼接，直到即将超出 <code>maxTokens</code> 限制为止。这种策略能有效避免短内容碎片化，同时保持原始 Markdown 块的完整性。若某个块本身已超长（例如一段未换行的代码说明或长引用），则交由下一层处理。</li>
<li><strong>句子级切分（<code>Paragraph</code> → <code>SplitIntoSentences</code> + <code>MergeSentencesToChunks</code>）</strong><br>
对于超长段落，我们不再暴力截断，而是先按中文句末标点（<code>。！？；……</code>）将其拆分为句子序列。随后，以贪心方式将这些句子重新组合成不超过 <code>maxTokens</code> 的文本块。虽然当前实现尚未引入滑动窗口重叠（overlap），但在绝大多数博客场景中，这种“句边界对齐”的切分已能很好地保留局部语义连贯性。</li>
</ol>
<p>整个过程完全基于 <strong>真实 token 计数</strong>（通过 Hugging Face Tokenizer 的 <code>Count</code> 接口），而非粗略的字符或字数估算，确保分块结果严格满足模型输入约束。</p>
<p>至此，从原始 Markdown 到可嵌入文本块的预处理流水线已全线贯通。</p>
<h1 id="8-向量库">8. 向量库</h1>
<p>现在，我们已经能将博客中的每一段文本转化为一个 512 维的语义向量。但若没有一种高效的方式组织和检索这些向量，语义搜索就无从谈起。</p>
<p>这正是向量库（Vector Database / Vector Index）的用武之地。它不关心文本内容，只专注于一件事：给定一个查询向量，在百万级向量池中快速找出最相似的若干个。</p>
<p>在资源极度受限的个人服务器上，我们无法部署重量级的向量数据库（如 Milvus 或 Weaviate）。经过权衡，最终选择了 FAISS —— 一个由 Meta 开源的轻量、高性能、纯 C++ 实现的向量相似性搜索库。它支持多种索引类型（如 IndexFlatIP、IndexIVFFlat、HNSW 等），既能满足精确搜索需求，也能通过近似算法大幅压缩内存与计算开销。</p>
<p>那么接下来，我们就批量把 Markdown 格式的博客文档向量化，存入到 FAISS 创建的向量库中。一般来说，这个功能是整个语义搜索系统的“离线构建”核心: 在部署前一次性将所有博客文章处理为向量，并持久化为 FAISS 索引文件，供线上搜索服务加载使用。具体代码如下：</p>
<pre><code class="language-cpp">// TaskEmbed.cpp
#include "TaskEmbed.h"

#include &lt;faiss/IndexFlat.h&gt;
#include &lt;faiss/IndexIDMap.h&gt;
#include &lt;faiss/index_io.h&gt;
#include &lt;gflags/gflags.h&gt;
#include &lt;onnxruntime_cxx_api.h&gt;

#include &lt;filesystem&gt;
#include &lt;fstream&gt;
#include &lt;iostream&gt;

#include "Application/Context.h"
#include "Embedding/BgeOnnxEmbedder.h"
#include "Embedding/MdSemanticSplitter.h"
#include "Embedding/TextChunker.h"
#include "LaunchConfig.h"
#include "LaunchInit.h"
#include "Persistence/Sqlite3Crud.h"
#include "Persistence/TableBlogChunksField.h"
#include "Persistence/TableBlogsField.h"

using namespace std;
using namespace embedding;
using namespace persistence;

DECLARE_string(input);

//所有博文向量化并存入向量库
namespace Bootstrap::Task::Embed {

void Run() {
  if (FLAGS_input.empty() /*|| FLAGS_output.empty()*/) {
    std::cerr &lt;&lt; "错误: sitemap 任务需要输入 --input 和 --output 参数\n";
    return;
  }

  filesystem::path launchConfigPath{FLAGS_input};
  if (!filesystem::exists(launchConfigPath)) {
    std::cerr &lt;&lt; "HTTP服务配置文件不存在！" &lt;&lt; endl;
    return;
  }

  if (!LaunchInit(launchConfigPath)) {
    return;
  }

  application::Context::GetInstance();

  string onnxModelPath = launchConfig.onnxModelPath;
  string faissIndexPath = launchConfig.faissIndexPath;

  std::filesystem::path modelPath(onnxModelPath);
  if (!std::filesystem::exists(onnxModelPath)) {
    std::cerr &lt;&lt; "Error: ONNX model file does not exist at: "
              &lt;&lt; modelPath.string() &lt;&lt; std::endl;
    return;
  }

  // 获取模型所在目录，并拼接 tokenizer.json
  std::filesystem::path tokenizerJsonFile =
      modelPath.parent_path() / "tokenizer.json";

  // 检查 tokenizer.json 是否存在（推荐）
  if (!std::filesystem::exists(tokenizerJsonFile)) {
    std::cerr &lt;&lt; "Warning: tokenizer.json not found at: "
              &lt;&lt; tokenizerJsonFile.generic_string() &lt;&lt; std::endl;
    return;
  }
  u8string tokenizerJsonFilePath = tokenizerJsonFile.generic_u8string();

  const size_t chunkMaxTokens = 396;

  vector&lt;Sqlite3Crud::TableValue&gt; result;
  std::vector&lt;TableBlogsField&gt; fieldNames{
      TableBlogsField::id,
      TableBlogsField::title,
      TableBlogsField::summary,
      TableBlogsField::content,
  };

  if (!Sqlite3Crud::Query&lt;TableBlogsField&gt;(TableName::blogs, fieldNames,
                                           result)) {
    return;
  }

  // 全量重建前，清空 chunk 表
  if (!Sqlite3Crud::Delete&lt;TableBlogChunksField&gt;(TableName::blog_chunks)) {
    std::cerr &lt;&lt; "清空 blog_chunks 表失败！\n";
    return;
  }
  std::cout &lt;&lt; "已清空 blog_chunks 表\n";

  MdSemanticSplitter mdSplitter;  //文档拆分器
  hf::Tokenizer tokenizer(
      (const char *)tokenizerJsonFilePath.c_str());    //初始化分词器
  TextChunker textChunker(chunkMaxTokens, tokenizer);  // token感知文本拆分
  BgeOnnxEmbedder embedder(onnxModelPath, tokenizer);

  int64_t embeddingDim = embedder.EmbeddingDim();
  auto flatIndex =
      std::make_unique&lt;faiss::IndexFlatIP&gt;(embeddingDim);  // 创建 FlatIP 索引
  faiss::IndexIDMap2 index(
      flatIndex.release());  // 自动接管 flatIndex 的生命周期

  size_t colNum = fieldNames.size();
  size_t rowNum = result.size() / colNum;

  for (size_t ri = 0; ri &lt; rowNum; ++ri) {
    size_t m = ri * colNum;
    string blogId = std::move(std::get&lt;std::string&gt;(result[m]));
    string title = std::move(std::get&lt;std::string&gt;(result[m + 1]));
    string summary = std::move(std::get&lt;std::string&gt;(result[m + 2]));
    string content = std::move(std::get&lt;std::string&gt;(result[m + 3]));

    // if (title != "C++中JSON序列化和反序列化的实现") {
    //  continue;
    //}

    cout &lt;&lt; ri &lt;&lt; '\t' &lt;&lt; title &lt;&lt; endl;

    std::vector&lt;SemanticBlock&gt; semanticBlocks =
        mdSplitter.Split(title, content, summary);

    // for (const auto&amp; block : semanticBlocks) {
    //  cout &lt;&lt; block.context &lt;&lt; endl;
    //  for (const auto&amp; content : block.contents) {
    //    cout &lt;&lt; content &lt;&lt; endl;
    //  }
    //  cout &lt;&lt; "----------------------\n";
    //}

    std::vector&lt;std::string&gt; chunks =
        textChunker.SemanticBlocks(std::move(semanticBlocks));

    // cout &lt;&lt; chunks.size() &lt;&lt; endl;
    // for (const auto&amp; chunk : chunks) {
    //  cout &lt;&lt; chunk;
    //  cout &lt;&lt; "-----------------------------\n";
    //}

    // 嵌入每个 chunk，并分配唯一 ID
    for (const auto &amp;chunk : chunks) {
      int64_t chunkId = Sqlite3Crud::InsertAndReturnId&lt;TableBlogChunksField&gt;(
          TableName::blog_chunks,
          {TableBlogChunksField::blog_id, TableBlogChunksField::title,
           TableBlogChunksField::chunk_text},
          {blogId, title, chunk});

      if (chunkId &lt;= 0) {
        std::cerr &lt;&lt; "插入 blog_chunks 失败\n";
        continue;
      }

      std::vector&lt;float&gt; embedding = embedder.Embed(chunk);
      index.add_with_ids(1, embedding.data(), &amp;chunkId);
    }
  }

  // 保存索引
  faiss::write_index(&amp;index, faissIndexPath.c_str());
}
}  // namespace Bootstrap::Task::Embed
</code></pre>
<p>可以把以上流程概括为三步：</p>
<ol>
<li>
<p><strong>数据读取</strong><br>
从 SQLite 数据库中全量拉取博客的 <code>id</code>、<code>title</code>、<code>summary</code> 和 <code>content</code>，作为原始输入。</p>
</li>
<li>
<p><strong>文本 → 向量</strong><br>
对每篇文章依次执行：</p>
<ul>
<li>用 <code>MdSemanticSplitter</code> 按标题层级拆分为语义段；</li>
<li>用 <code>TextChunker</code> 将语义段切分为不超过 396 tokens 的上下文感知文本块（预留空间给 <code>[CLS]</code>/<code>[SEP]</code> 等特殊 token）；</li>
<li>调用 <code>BgeOnnxEmbedder</code> 为每个 chunk 生成 512 维 L2 归一化的 embedding。</li>
</ul>
</li>
<li>
<p><strong>向量 → 索引</strong><br>
使用 FAISS 的 <code>IndexFlatIP</code>（精确内积索引，等价于余弦相似度）构建向量库，并通过 <code>IndexIDMap2</code> 将每个向量关联到其在数据库中的唯一 <code>chunk_id</code>。最终，索引连同 ID 映射一起写入磁盘（如 <code>faiss.index</code>）。</p>
</li>
</ol>
<p>此外，每个 chunk 的元信息（所属文章 ID、标题、原始文本）被存入 <code>blog_chunks</code> 表，实现<strong>向量 ID 到原文内容的反查</strong>——这是搜索结果展示的关键桥梁。</p>
<p>至此，一个轻量、自包含、无外部依赖的语义向量库就构建完成了。</p>
<h1 id="9-业务应用">9. 业务应用</h1>
<p>最后，就是运用这个向量库去实现具体的业务应用了。</p>
<h2 id="91-初始化">9.1 初始化</h2>
<p>当启动后端时，需要初始化相关的内容：</p>
<pre><code class="language-cpp">// EmbedComponent.h
#pragma once
#include "Embedding/BgeOnnxEmbedder.h"
#include "Embedding/MdSemanticSplitter.h"
#include "Embedding/NotDeletedIdSelector.h"
#include "Embedding/TextChunker.h"

namespace faiss {
struct Index;
}

namespace application {
struct EmbedComponent {
  explicit EmbedComponent(const std::string&amp; tokenizerJsonFilePath,
                          size_t chunkMaxTokens,
                          const std::string&amp; onnxModelPath,
                          const std::string&amp; faissIndexPath);
  ~EmbedComponent();

  embedding::MdSemanticSplitter mdSplitter;    // 文档拆分器
  embedding::hf::Tokenizer tokenizer;          // Hf分词器
  embedding::TextChunker textChunker;          // token感知文本拆分
  embedding::BgeOnnxEmbedder embedder;         // 文本嵌入
  std::unique_ptr&lt;faiss::Index&gt; index;         // 向量库
  embedding::NotDeletedIdSelector idSelector;  // 记录删除的ID
};
}  // namespace application
</code></pre>
<pre><code class="language-cpp">// EmbedComponent.cpp
#include "EmbedComponent.h"

#include &lt;faiss/Index.h&gt;
#include &lt;faiss/index_io.h&gt;

namespace application {
EmbedComponent::EmbedComponent(const std::string&amp; tokenizerJsonFilePath,
                               size_t chunkMaxTokens,
                               const std::string&amp; onnxModelPath,
                               const std::string&amp; faissIndexPath)
    : tokenizer((const char*)tokenizerJsonFilePath.c_str()),
      textChunker(chunkMaxTokens, tokenizer),
      embedder(onnxModelPath, tokenizer),
      index(faiss::read_index(faissIndexPath.c_str())) {}

EmbedComponent::~EmbedComponent() = default;

}  // namespace application
</code></pre>
<h2 id="92-搜索">9.2 搜索</h2>
<p>当用户进行搜索时：</p>
<pre><code class="language-cpp">#include "Search.h"

#include &lt;faiss/Index.h&gt;
#include &lt;faiss/index_io.h&gt;

#include &lt;iostream&gt;
#include &lt;set&gt;

#include "Application/Context.h"
#include "Application/EmbedComponent.h"
#include "Domain/SearchHit.h"
#include "Embedding/BgeOnnxEmbedder.h"
#include "Persistence/Sqlite3Crud.h"
#include "Persistence/TableBlogChunksField.h"

using namespace std;
using namespace embedding;
using namespace persistence;
using namespace domain;
using namespace application;

namespace Service {

namespace search {

bool Posts(const std::string&amp; query,
           std::vector&lt;domain::SearchHit&gt;&amp; searchHits) {
  EmbedComponent* embedComponent = Context::GetInstance().GetEmbedComponent();

  const BgeOnnxEmbedder&amp; embedder = embedComponent-&gt;embedder;
  const faiss::Index* index = embedComponent-&gt;index.get();

  std::vector&lt;float&gt; queryVec = embedder.Embed(query);

  constexpr int k = 200;  // 返回 top200
  std::vector&lt;faiss::idx_t&gt; indices(k);
  std::vector&lt;float&gt; distances(k);

  faiss::SearchParameters params;
  params.sel = &amp;(embedComponent-&gt;idSelector);
  index-&gt;search(1, queryVec.data(), k, distances.data(), indices.data(),
                &amp;params);

  // 输出结果
  set&lt;string&gt; blogIdSet;
  for (int i = 0; i &lt; k; ++i) {
    if (indices[i] &lt; 0) {
      continue;
    }

    if (distances[i] &lt; 0.5) {
      break;
    }
    // std::cout &lt;&lt; "Chunk ID: " &lt;&lt; indices[i] &lt;&lt; "\tScore: " &lt;&lt; distances[i]
    //          &lt;&lt; "\n";

    int64_t chunkId = indices[i];
    std::vector&lt;Sqlite3Crud::TableValue&gt; result;
    Sqlite3Crud::Query&lt;TableBlogChunksField&gt;(
        TableName::blog_chunks,
        {TableBlogChunksField::blog_id, TableBlogChunksField::title,
         TableBlogChunksField::chunk_text},
        result, {TableBlogChunksField::chunk_id}, {chunkId});

    string blogId = std::move(std::get&lt;string&gt;(result.at(0)));
    if (blogIdSet.find(blogId) == blogIdSet.end()) {
      blogIdSet.insert(blogId);
      SearchHit searchHit;
      searchHit.id = std::move(blogId);
      searchHit.title = std::move(std::get&lt;string&gt;(result.at(1)));
      searchHit.snippetText = std::move(std::get&lt;string&gt;(result.at(2)));
      searchHits.emplace_back(std::move(searchHit));
    }
  }

  return true;
}

}  // namespace search
}  // namespace Service
</code></pre>
<p>有了预构建的向量库和配套的元数据表，真正的语义搜索就变得异常简洁：<strong>将用户查询也嵌入为向量，然后在 FAISS 索引中执行近似最近邻（ANN）查找即可</strong>。</p>
<p>整个在线搜索流程分为三步：</p>
<ol>
<li><strong>查询嵌入</strong><br>
用户输入的搜索词（如“git撤回提交”）通过与构建阶段完全相同的 <code>BgeOnnxEmbedder</code> 流程——包括 Hugging Face 分词、ONNX 推理、L2 归一化——转化为一个 512 维的单位向量。这确保了查询向量与文档向量处于同一语义空间。</li>
<li><strong>向量检索</strong><br>
将查询向量送入 FAISS 索引，执行 <code>index.search()</code>。我们使用内积（IP）作为相似度度量（因向量已 L2 归一化，IP 等价于余弦相似度），并请求 Top-K（例如 K=200）最相似的 chunk ID 及其得分。</li>
<li><strong>结果组装与去重</strong><br>
根据返回的 chunk ID，从 <code>blog_chunks</code> 表中查出对应的原始文本、所属文章标题和博客 ID。为避免同一篇文章因多个 chunk 被重复召回，我们按 <code>blog_id</code> 去重，优先保留得分最高的片段，并将其作为搜索结果的摘要（snippet）呈现给用户。</li>
</ol>
<h2 id="93-新增">9.3 新增</h2>
<p>批量向量化所有的博文成本太高，最好是能够实现增量更新，如下所示：</p>
<pre><code class="language-cpp">// 为博客生成并添加向量嵌入（用于语义搜索）
static void AddBlogEmbedding(const string&amp; blogId, const string&amp; title,
                             const string&amp; content, const string&amp; summary) {
  //文档拆分
  const EmbedComponent* embedComponent =
      Context::GetInstance().GetEmbedComponent();
  std::vector&lt;std::string&gt; chunks = embedComponent-&gt;textChunker.SemanticBlocks(
      std::move(embedComponent-&gt;mdSplitter.Split(title, content, summary)));

  // 嵌入每个 chunk，并分配唯一 ID
  for (const auto&amp; chunk : chunks) {
    int64_t chunkId = Sqlite3Crud::InsertAndReturnId&lt;TableBlogChunksField&gt;(
        TableName::blog_chunks,
        {TableBlogChunksField::blog_id, TableBlogChunksField::title,
         TableBlogChunksField::chunk_text},
        {blogId, title, chunk});

    if (chunkId &lt;= 0) {
      std::cerr &lt;&lt; "插入 blog_chunks 失败\n";
      continue;
    }

    std::vector&lt;float&gt; embedding = embedComponent-&gt;embedder.Embed(chunk);
    embedComponent-&gt;index-&gt;add_with_ids(1, embedding.data(), &amp;chunkId);
  }

  // 创建新线程来写入，提升响应时间
  // Todo：后期考虑移植到定时任务中，并且需要保证推出后正常写出
  std::thread writeIndexThread([embedComponent]() {
    faiss::write_index(embedComponent-&gt;index.get(),
                       launchConfig.faissIndexPath.c_str());
  });

  // 确保主线程不等待此线程完成，避免阻塞
  writeIndexThread.detach();
}
</code></pre>
<p>这也是为什么使用 <code>IndexIDMap2</code> 的原因：可以将新增的向量关联到其在数据库中元数据的唯一 <code>chunk_id</code> 上。由于在初始化时已经将向量索引读取到内存中，在新增向量后只需要再将向量索引对象保存即可。</p>
<h2 id="94-删除">9.4 删除</h2>
<p>删除的实现有点麻烦，因为 FAISS 的 <code>IndexFlatIP</code> 没有提供删除的接口。当然这也不是设计缺陷，而是数据结构就是这么设计的，为了保证检索的性能必然有所舍弃。因此，要实现删除只能软删除，也就是在元数据中标记一下相应的 chunk 已经被删除了。同时，要注意到前面在搜索的实现中，<code>index.search()</code> 传入一个自定义的 <code>IDSelector</code>参数对象：</p>
<pre><code class="language-cpp">// NotDeletedIdSelector.h
#pragma once

#include &lt;faiss/impl/IDSelector.h&gt;

#include &lt;unordered_set&gt;

namespace embedding {

/// @brief 定义一个 selector：只保留“未被删除”的 ID
class NotDeletedIdSelector : public faiss::IDSelector {
 public:
  // 构造时传入“已删除的 ID 集合”
  // explicit NotDeletedIDSelector();

  // 重写 is_member：如果 id 在 deletedSet 中，返回 false（屏蔽）
  bool is_member(faiss::idx_t id) const override;

  void Add(faiss::idx_t id);

 private:
  std::unordered_set&lt;faiss::idx_t&gt; deletedSet;
};

}  // namespace embedding
</code></pre>
<pre><code class="language-cpp">// NotDeletedIdSelector.cpp
#include "NotDeletedIdSelector.h"

namespace embedding {

// 重写 is_member：如果 id 在 deletedSet 中，返回 false（屏蔽）
bool NotDeletedIdSelector::is_member(faiss::idx_t id) const {
  return deletedSet.find(id) == deletedSet.end();
}

void NotDeletedIdSelector::Add(faiss::idx_t id) { deletedSet.insert(id); }

}  // namespace embedding
</code></pre>
<p>这个 <code>NotDeletedIdSelector</code> 对象传入 <code>index.search()</code> 时会动态过滤掉已被标记删除的 chunk ID，实现“逻辑删除”而无需物理重建索引。正因为如此，在删除操作时，需要记录标记删除的 chunk ID：</p>
<pre><code class="language-cpp">// 从向量索引中移除博客的嵌入，并标记 chunk 为已删除
static void RemoveBlogEmbedding(const std::string&amp; blogId) {
  std::vector&lt;Sqlite3Crud::TableValue&gt; result;
  Sqlite3Crud::Query&lt;TableBlogChunksField&gt;(
      TableName::blog_chunks, {TableBlogChunksField::chunk_id}, result,
      {TableBlogChunksField::blog_id}, {blogId});

  EmbedComponent* embedComponent = Context::GetInstance().GetEmbedComponent();
  for (const auto&amp; value : result) {
    embedComponent-&gt;idSelector.Add(std::get&lt;int64_t&gt;(value));
  }

  Sqlite3Crud::Update&lt;TableBlogChunksField&gt;(
      TableName::blog_chunks,
      {TableBlogChunksField::is_deleted, TableBlogChunksField::blog_id},
      {1LL, blogId});
}
</code></pre>
<p>当然，在初始化时也要记录这些被标记删除的 chunk ID：</p>
<pre><code class="language-cpp">std::vector&lt;Sqlite3Crud::TableValue&gt; result;
Sqlite3Crud::Query&lt;TableBlogChunksField&gt;(
    TableName::blog_chunks, {TableBlogChunksField::chunk_id}, result,
    {TableBlogChunksField::is_deleted}, {1});

for (const auto &amp;value : result) {
  embedComponent-&gt;idSelector.Add(std::get&lt;int64_t&gt;(value));
}
</code></pre>
<h2 id="95-更新">9.5 更新</h2>
<p>如果要更新博文嵌入的相关内容，由于嵌入操作的复杂性，直接删除后新增即可，直接复用前面的软删除和增量更新：</p>
<pre><code class="language-cpp">RemoveBlogEmbedding(blogId);
AddBlogEmbedding(blogId, blogMeta.title, blogData.content, blogMeta.summary);
</code></pre>
<h1 id="10-优化提升">10. 优化提升</h1>
<p>至此，一个从零构建、端到端的轻量级语义搜索系统便完整落地。整个搜索过程不依赖任何外部服务，仅需加载一次 FAISS 索引和 ONNX 模型，即可在毫秒级内响应查询。在 1GB 内存的云服务器上，内存占用稳定在 200MB 以内（后端程序+嵌入模型+向量库），完美契合“极致性能 + 极致资源效率”的初始目标。在性能和效率方面笔者已经比较满意了，有时间就测试一下具体的性能参数以及与 Python 生态的对比。</p>
<p>不过，由于实现的比较仓促，其实还有很多可以优化和提升的地方：</p>
<h2 id="101-混合检索">10.1 混合检索</h2>
<p>尽管基于向量的语义搜索在理解用户意图、处理同义词和模糊表达方面展现出强大能力，但它并非万能：</p>
<ul>
<li><strong>细节丢失问题</strong>：语义嵌入倾向于捕捉整体语义，而对精确关键词敏感度较低。当用户明确知道要找某个具体术语时，语义模型可能因“过度泛化”而将相关但不精确的结果排在前面，反而漏掉真正匹配的文档。</li>
<li><strong>长尾查询表现不稳定</strong>：对于罕见术语、专业缩写或拼写变体，嵌入模型若未在训练数据中充分覆盖，其向量表示可能偏离真实语义，导致检索失效。</li>
<li><strong>可控性弱</strong>：传统关键词搜索可通过布尔逻辑（AND/OR/NOT）、字段限定（title:xxx）、通配符等提供精细控制，而纯向量搜索缺乏此类机制，难以满足高级用户的精确筛选需求。</li>
</ul>
<p>因此，<strong>单一依赖语义搜索并不足以覆盖全部搜索场景</strong>。更合理的方案是构建<strong>混合检索系统（Hybrid Search）</strong>：将<strong>关键词匹配（如 BM25）</strong> 与 <strong>向量相似度检索</strong> 的结果进行融合——例如通过加权打分（Reciprocal Rank Fusion）、重排序（Rerank）或前端分 Tab 展示。这样既能利用语义搜索理解“撤回 Git 提交”等自然语言查询，又能确保“<code>git revert HEAD</code>”这类精确命令被准确命中。</p>
<h2 id="102-分块器强化">10.2 分块器强化</h2>
<p>在大部分情况下，这里分块器 <strong>结构感知 + 句子对齐 + 上下文注入</strong> 的三重策略，已在效果与效率之间取得了令人满意的平衡。不过，在更复杂的 RAG 系统中，可能还需要以下优化：</p>
<ul>
<li>内容类型感知分块：对代码块、表格、LaTeX 公式等非连续文本进行特殊处理。例如，将完整代码块视为不可分割单元；对表格按行或语义列拆分；避免在公式中间强行切分。</li>
<li>动态语义边界检测：引入轻量级语言模型（如 Sentence-BERT 或专用边界分类器）识别段落内部的语义转折点，实现更自然的分段，而非仅依赖标点或固定长度。</li>
<li>滑动窗口重叠机制：在固定最大 token 长度（如 512）基础上，采用滑动窗口（如步长 384，重叠 128 tokens）生成连续重叠块。这能缓解因硬性切分导致的关键信息被割裂的问题，尤其适用于长技术说明或连贯论证段落。</li>
<li>列表与枚举项精细化处理：当前分块器可能将多行无序/有序列表整体保留，但若列表项本身较长且独立，可进一步按项拆分，并为每项注入所属标题上下文，提升细粒度检索能力。</li>
<li>跨块上下文链接：在存储 chunk 时，额外记录其前驱/后继 chunk ID，便于在后续 RAG 阶段拼接上下文，还原更完整的原始语境。</li>
</ul>
<p>这些改进并非都要同时引入，而应根据实际内容分布和用户查询模式逐步迭代。毕竟，分块的本质是在“信息密度”、“语义完整性”与“检索效率”之间寻找最优平衡点——而这个平衡点，会随着业务演进而不断移动。</p>
<h2 id="103-高亮溯源">10.3 高亮溯源</h2>
<p>当前搜索结果虽已返回 <code>snippetText</code>（即命中的文本片段），但用户仍需自行在原文中定位关键词位置，体验不够直观。为提升可用性，应实现<strong>关键词高亮与片段溯源</strong>功能。</p>
<p>其实现可分为两步：</p>
<ol>
<li>
<p><strong>命中词提取与高亮</strong><br>
在返回 <code>snippetText</code> 前，对原始查询进行分词（复用 <code>Tokenizer</code>），提取核心关键词（或使用 BM25 逆向匹配高频 term），然后在 <code>snippetText</code> 中进行不区分大小写的子串匹配，并用 <code>&lt;mark&gt;</code> 或 <code>**</code> 等标记包裹命中词。例如：</p>
<blockquote>
<p>“如何<strong>撤回</strong> Git <strong>提交</strong>” → “...可以使用 <code>git revert</code> 来<strong>撤回</strong>某次<strong>提交</strong>...”</p>
</blockquote>
</li>
<li>
<p><strong>上下文扩展与锚点定位</strong><br>
当前 <code>snippetText</code> 仅为 chunk 内容，可能缺乏足够上下文。可进一步根据 chunk 所属的段落结构（如 Markdown 标题层级），向前/后扩展 1–2 句作为补充上下文，并在前端通过滚动锚点（如 <code>#chunk-12345</code>）直接跳转到原文对应位置，实现“所见即所达”的溯源体验。</p>
</li>
</ol>
<p>高亮溯源虽是细节，却是连接“检索结果”与“用户信任”的关键一环——让用户一眼看清“为什么这条结果被召回”，从而提升搜索系统的透明度与可信度。</p>
<h2 id="104-向量索引">10.4 向量索引</h2>
<p>在目前小规模场景（通常几百到几千个 chunk），使用 <code>IndexFlatIP</code> 精确搜索的开销完全可接受，且能保证最高召回质量。若未来数据量激增，可无缝替换为 <code>IndexIVFFlat</code> 或 <code>HNSW</code> 等近似索引，而上层接口不变。</p>
<p>为了便于后续演进，这里给出一个基于数据规模的经验性升级路径建议：</p>
<table>
<thead>
<tr>
<th><strong>Chunk 数量范围</strong></th>
<th><strong>推荐 FAISS 索引类型</strong></th>
<th><strong>特点与适用场景</strong></th>
<th><strong>典型参数（示例）</strong></th>
<th><strong>是否支持动态插入</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>≤ 10,000</td>
<td><code>IndexFlatIP</code></td>
<td>精确搜索，实现简单，延迟极低（&lt;10ms），适合小规模静态知识库</td>
<td>无</td>
<td>✅ 是</td>
</tr>
<tr>
<td>10,000 ~ 100,000</td>
<td><code>IndexIVFFlat</code></td>
<td>近似搜索，通过聚类加速，召回率高（&gt;95%），适合中等规模、需频繁增量更新的场景</td>
<td><code>nlist=100</code>, <code>nprobe=10</code></td>
<td>✅ 是</td>
</tr>
<tr>
<td>&gt; 100,000</td>
<td><code>IndexHNSW</code></td>
<td>图结构近似搜索，查询速度极快且几乎不随数据增长变慢，适合大规模、高并发只读场景</td>
<td><code>M=32</code>, <code>efConstruction=200</code>, <code>efSearch=64</code></td>
<td>❌ 否（FAISS 默认不支持）</td>
</tr>
<tr>
<td>&gt; 1,000,000（超大规模）</td>
<td><code>IndexIVFPQ</code> 或 <code>HNSW+PQ</code></td>
<td>引入向量量化（Product Quantization）压缩内存，牺牲少量精度换取更低内存占用和更快检索</td>
<td><code>nlist=1000</code>, <code>pq.M=64</code>, <code>pq.nbits=8</code></td>
<td>✅（IVF 支持）</td>
</tr>
</tbody>
</table>
<h2 id="105-业务性能">10.5 业务性能</h2>
<p>在涉及博文的增删改（CRUD）操作时，<strong>嵌入生成与索引更新属于辅助性后台任务</strong>，并不影响主业务流程（如文章发布、编辑保存等）。用户的核心诉求是“操作即时响应”，而对搜索结果的收录延迟通常具有较高容忍度——几秒甚至几分钟的滞后在大多数场景下是可以接受的。</p>
<p>因此，<strong>不应在主线程中同步执行嵌入计算与 FAISS 写入</strong>，否则会显著拖慢 HTTP 响应时间，尤其当 ONNX 推理或磁盘 I/O 成为瓶颈时。</p>
<p>推荐采用以下策略之一：</p>
<ul>
<li><strong>异步任务队列（推荐）</strong>：将嵌入任务（如 <code>AddBlogEmbedding</code> / <code>RemoveBlogEmbedding</code>）推入内存队列或轻量级任务调度器（如基于 <code>std::queue</code> + 单独工作线程），由后台线程按序处理。这样既能保证主线程快速返回，又能避免多线程并发写 FAISS 索引引发的数据竞争（FAISS 的 <code>Index</code> 默认非线程安全）。</li>
<li><strong>协程或异步 I/O（进阶）</strong>：若系统已基于支持协程的框架（如 Seastar、Boost.Asio + coroutine TS），可将嵌入与索引写入封装为 suspendable 任务，在不阻塞事件循环的前提下完成耗时操作。但考虑到当前系统轻量级定位，此方案收益有限，复杂度较高。</li>
<li><strong>定期批量更新（兜底方案）</strong>：对于低频更新场景，也可暂存待处理的 blog ID 到数据库标记表，由定时任务（如每 5 分钟）统一扫描并批量执行嵌入与索引更新。这种方式实现简单，且天然规避并发问题，适合 MVP 或资源受限环境。</li>
</ul>
<p>此外，FAISS 索引的持久化（<code>faiss::write_index</code>）属于 I/O 密集型操作，建议在后台任务完成所有向量添加后<strong>一次性写入</strong>，而非每次新增 chunk 都触发写盘，以减少磁盘压力并提升吞吐。</p>
<p>通过上述异步化设计，系统既能维持毫秒级的前端响应，又能稳健地保障搜索数据的逐步收敛，真正实现“用户体验”与“系统效率”的双赢。</p>


</div>
<div class="clear"></div>

            </div>
            <div class="postDesc">posted @ 
<span id="post-date" data-last-update-days="0" data-date-updated="2026-02-12 21:42">2026-02-12 21:41</span>&nbsp;
<a href="https://www.cnblogs.com/charlee44">charlee44</a>&nbsp;
阅读(<span id="post_view_count">7</span>)&nbsp;
评论(<span id="post_comment_count">0</span>)&nbsp;
&nbsp;
<a href="javascript:void(0)" onclick="AddToWz(19609813);return false;">收藏</a>&nbsp;
<a href="javascript:void(0)" onclick="reportManager.report({ currentUserId: '', targetType: 'blogPost', targetId: '19609813', targetLink: 'https://www.cnblogs.com/charlee44/p/19609813', title: '从零实现一个生产级 RAG 语义搜索系统：C++ + ONNX + FAISS 实战' })">举报</a>
</div>
        